# 开发约定与流程

更新日期：2026-08-08

## 代码边界

- 仓库维护 BepInEx Mod 与 Tauri 伴随窗口。
- 伴随窗口入口为 `apps/companion/src/companion/ModWorkbench.tsx`，顶层挂载在 `apps/companion/src/App.tsx`。
- 推荐算法核心集中在 `apps/companion/src/recommendation-engine/`；经营中订单推荐由 `apps/companion/src/companion/domain/service-recommendations.ts` 组装，并通过 `apps/companion/src/companion/workers/order-recommendations.worker.ts` 放到 worker 中计算。经营中普客订单的执行方案详情由 `apps/companion/src/companion/domain/normal-order-details.ts` 派生，也必须通过独立的经营中订单推荐 worker 请求按需计算，不得放回 React render 链路，也不得影响稀客订单推荐和自动化使用的 `pending/isCurrent` 门禁。完整经营中订单推荐 worker 不得因为自动化总开关或普客自动化单独开启就在所有页面后台运行；只有经营中页面、专注模式、任一游戏界面辅助或稀客自动化实际需要推荐候选时才启用。主推荐请求和普客详情请求都必须用订单、库存、厨具、特殊经营上下文和相关推荐偏好的语义签名稳定输入；不要让 `/snapshot` 轮询产生的新对象引用反复触发 worker 投递、结构化克隆或整列表重绘。推荐 worker hook 必须使用 active + latest queued 请求模型，快照连续刷新时只投递最新排队请求，避免重计算响应被 750ms 轮询持续冲掉。订单推荐 worker 和页面推荐 worker 都要按 `RecommendationDataSet` 语义签名缓存完整运行时目录，只有签名变化或 worker 重新初始化时才随请求发送完整数据集，后续请求只传业务 payload 和数据签名，避免自动化轮询导致大对象反复结构化克隆。普客页和稀客页的页面推荐通过 `apps/companion/src/companion/workers/page-recommendations.worker.ts` 计算，页面组件只负责选择状态和结果渲染，不得把候选搜索、排序或自定义料理合并重新放回 React render 链路。顶层工作台的重型页面内容只允许当前 active tab 挂载，inactive 页面不得常驻完整运行时目录派生列表或页面 worker；页面推荐 hook 在 payload 为空时必须 terminate worker 并清空 worker 数据签名。
- 推荐、库存名称和自动化目标使用 Mod 从游戏运行时读取并通过本地 API 发布的结构化数据；运行时数据未就绪时，伴随窗口显示等待状态。
- C# Mod 不引用 TypeScript 模块；前端和 Mod 的共享数据通过本地 API 的运行时快照传递。新增稀客事件变体时，优先确认游戏运行时映射和别名归一化逻辑。

## 命名约束

- 项目、产品名、安装目录、发布产物和用户可见项目引用统一使用 `mystia-steward-companion`。
- C# 命名空间和类型可使用 `MystiaStewardCompanion`。
- 旧名称只允许出现在明确的兼容迁移代码或上游来源说明中，例如旧 BepInEx 配置和旧 localStorage key 迁移。
- 游戏角色、场景和挑战名称优先使用游戏运行时数据或程序集元数据中的正式名称。用户可见文案、文档和诊断不得使用社区昵称；英文标识只用于内部类型、字段、日志 key 或中文元数据不可读时的原始诊断。
- 修改路径或项目名时，必须同步更新 README、AGENTS、构建脚本、GitHub Actions 和相关 docs。
- IL2CPP 分析固定使用 `mods/bepinex/tools/il2cpp-analysis/` 的锁定工具链，输出放在仓库外的
  `/huyu/data/disk/mystia-steward-companion/new/`。运行时行为依次用 metadata C#、BepInEx #783 interop、
  IDA/Hex-Rays 和实机日志交叉验证；旧分析只在 `backup/` 做历史对比，不建立新旧 fallback。Cpp2IL
  `2022.1.0-pre-release.21` 的 `dll_il_recovery` 只生成 `ldnull; throw`，不得恢复或引用为方法源码。

## 编码规范

- TypeScript 使用 strict 写法，避免 `any`。
- `src` 内导入统一使用 `@/` 别名。
- React 代码使用函数组件和 hooks。
- 面向用户的文案默认使用中文；Mod UI 需要同时保留中文和英文入口。
- 不在组件中硬编码平衡值，优先更新结构化数据和类型化逻辑。
- 伴随窗口 UI 基础组件统一放在 `apps/companion/src/components/ui/`。按钮、输入框、选择框、页签、卡片、徽标、开关、滑杆、选项组、折叠面板、状态卡片、空状态和信息字段都优先使用该目录组件，不要在业务页面复制外部模板组件或手写第二套样式。
- UI 原语以 Mantine 组件为交互基础，通过 `apps/companion/src/components/ui/` 和 `components/ui-kit` 做项目级适配；样式由 Mantine theme、项目 CSS token 和少量尺寸归一 wrapper 控制。新增组件要保持工具型窗口的紧凑布局、企业控制台式扁平分组、直角窄边框、弱动画和可读高对比；除开关、滚动条滑块等需要圆形几何的控件外，不再使用圆角。列表和推荐优先使用表格化行项目，不引入通用后台模板、玻璃拟态、过度圆角、卡片堆叠或独立视觉体系。
- 伴随窗口布局必须覆盖 Tauri 主窗口最小宽度 640px。常规双列密集面板应在 640px 及以上保持双列，仅低于桌面最小窗口限制时退回单列；640-719px 使用紧凑窄桌面布局，Header 品牌区可以与连接工具条分行，但连接工具条内部的 endpoint、Token、连接和刷新必须保持单行。设置专用 SegmentedControl 必须按内容收缩、标签保持单行，不得强制全宽等分或任意断词；通过紧凑内边距覆盖 640px/130% 字号。页面工具条只有在不破坏标题与动作归属时才允许换行。三项状态摘要必须保持三列，一级导航必须以五列两行完整显示。只有低于 640px 的移动窄屏才允许状态摘要纵排、连接工具条内部换行和一级导航横向滚动。滑杆和表单控件不得通过提高窗口最小宽度、隐藏关键信息或横向溢出来规避问题；UI 巡检需包含 640px 最小宽度截图及上述几何断言。
- 页面级代码应继续按页面和业务域拆分到 `apps/companion/src/companion/pages/`、`domain/`、`hooks/`、`features/` 或 `workers/`。新增页面时先复用 `ListPanel`、`InfoLine`、`EmptyState`、`SwitchField`、`SliderField`、`SegmentedControl`、`Tree` 和 `Accordion`，避免页面层样式混乱。

## 构建验证

当前 Linux 验证基线为 Node.js `24.19.0`、Corepack `0.35.0`、仓库固定 pnpm `10.10.0`、
.NET SDK `10.0.110` 和 Rust `1.97.1`。Windows 开发机建议对齐这些主版本；Mod 发布目标仍保持
`net6.0`，不因本机构建 SDK 升级而改目标框架。只有 .NET 10 runtime 的环境运行 net6 smoke 时可为当前
测试进程设置 `DOTNET_ROLL_FORWARD=Major`，不要安装已停止支持的 .NET 6 SDK 作为项目固定依赖。
`automation-cooking-job` 含真实 Harmony/MonoMod 动态补丁探针；该旧依赖组合在 Linux .NET 10 CoreCLR
上会于探针执行时发生原生崩溃，因此此项必须在固定 .NET 6 SDK 容器中运行，不能删除探针或用源码断言
替代运行时验证：

```bash
docker run --rm -v "$PWD:/workspace" -w /workspace \
  mcr.microsoft.com/dotnet/sdk@sha256:eaa148f3f58a7276c9bb2eac0612f9560e9ba69e44de02e9ae1912e7105a9b1e \
  dotnet run --project tests/automation-cooking-job/AutomationCookingJobSmoke.csproj -c Release
```

常规检查：

```bash
pnpm lint
pnpm build
```

构建产物空间检查和清理：

```bash
pnpm artifacts:report
pnpm artifacts:prune
pnpm artifacts:clean -- --dry-run
```

产物管理器只操作仓库内白名单中的可再生 Cargo、Gradle、Vite 和 .NET 构建目录；默认在总量超过 12 GiB 时清理到 8 GiB，Android 与 .NET 分类上限分别为 1.5 GiB 和 0.5 GiB，并优先移除超过 14 天的非活动完整缓存单元。不得按单个 Cargo hash、`deps` 文件或文件数量裁剪缓存，也不得自动删除 `mods/bepinex/dist`、`mods/bepinex/References`、`temp`、`node_modules`、`.playwright-cli` 或 Android 签名材料。完整发布资产复制完成后才允许清理对应中间目录。

配额使用文件逻辑大小，不依赖平台特有的磁盘块统计。常用 Tauri 桌面/Android 命令会在启动构建前执行 prune；直接运行 Cargo 或 dotnet 命令后仍需显式检查。`prune`/`clean` 自身使用独占锁，但手动调用时仍必须确认 Cargo、Gradle、Vite 和 dotnet 构建均已退出。发布目录事务与缓存清理审计分别运行 `pnpm audit:release-package` 和 `pnpm audit:build-artifacts`。

伴随窗口：

```bash
pnpm tauri:build
```

BepInEx 插件：

```bash
dotnet build mods/bepinex/MystiaStewardCompanion.BepInEx.csproj -c Release
```

一键发布包：

```powershell
pwsh -ExecutionPolicy Bypass -File mods\bepinex\tools\build-release.ps1
```

该命令会生成发布包；除非用户明确要求，不要运行。

## GitHub Actions 与发布

- `.github/workflows/ci.yml` 仅支持手动触发，用于前端 lint 和 build 检查。
- 仓库不使用 GitHub Actions 自动构建 Release；不要新增 tag 自动构建 workflow。
- 版本发布采用本机 Windows 构建后通过 `gh` 上传，详细说明见 `docs/local-release.md`。
- 自动更新发布只支持稳定版 `X.Y.Z` 和预览版 `X.Y.Z-preview.N`。预览版必须发布为 GitHub Prerelease，用于 `dev` 上验证自动更新链路；稳定版确认后再合并 `main` 并发布普通 Release。
- GitHub Release 需要上传 Mod 主包、`update-manifest.json` 和独立 Windows x64 伴随窗口 EXE：`mystia-steward-companion-companion-windows-x64.exe`；如发布机已配置 Android 工具链和签名配置，可通过 `build-release.ps1 -BuildAndroidApk` 或 `publish-release.ps1 -BuildAndroidApk` 生成并上传按 ABI 拆分的 Android APK，默认资产为 `mystia-steward-companion-android-arm64-v8a.apk` 和 `mystia-steward-companion-android-armeabi-v7a.apk`。`update-manifest.json` 只服务 Mod 自动更新，必须继续指向 `mystia-steward-companion-bepinex.zip`，不要把独立伴随窗口 EXE 或 Android APK 纳入自动更新清单。
- `mods/bepinex/dist` 是本次发布资产的生成目录。正常 package/build 必须先在 staging 中完成并验证本次资产，再替换旧目录，不能保留上次 APK、manifest、tar、zip 或旧目录混入新发布；`publish-release.ps1 -SkipBuild` 是显式复用已有完整资产的唯一例外。构建缓存配额不包含 `dist`，也不得为了达成配额静默删除发布资产。
- package/APK 事务遗留的 `dist.staging-*`、`dist.backup-*` 或 `.android-apk-stage-*` 不能自动覆盖或继续叠加；后续构建必须 fail-closed 并列出路径，由开发者确认 canonical `dist` 后恢复或删除。覆盖已有 GitHub Release 时还要对账两个 canonical Android APK，本次不再发布的旧资产只有显式 `-Clobber` 时才能删除。
- 不要主动创建 tag 或发布 Release；版本构建必须等待用户明确指令。
- 用户和测试文档中的 BepInEx 安装版本优先固定到已验证的 `BepInEx-Unity.IL2CPP-win-x64-6.0.0-be.783+c58c42d.zip`。不要笼统推荐最新 Bleeding Edge；#784 及之后构建若要恢复支持，需要先通过实测和运行时日志确认。
- Android APK 不是 Windows 伴随窗口 EXE 的转换产物。Android 版按 Tauri mobile 目标维护，只作为 B 设备 LAN 伴随窗口；桌面托盘、置顶、鼠标穿透、焦点切换、单实例控制和游戏关闭自动退出必须继续隔离在桌面平台代码中。Android applicationId 固定为 `com.tyukki.mystia.steward.companion`；不要使用带连字符的产品名作为 Android 包名。桌面 Tauri identifier 继续使用既有值，Android 通过 `apps/companion/src-tauri/tauri.android.conf.json` 单独覆盖 identifier，避免影响桌面端本地数据目录。仓库保留 `apps/companion/src-tauri/gen/android/` 工程，Gradle Rust 插件必须通过 Corepack 调用 pnpm。Android 发布 APK 默认通过 `--split-per-abi --target aarch64 armv7` 构建，避免 universal fat APK；`pnpm tauri:android:apk:signed` 读取被 Git 忽略的 `apps/companion/src-tauri/gen/android/keystore.properties`，构建后用 `apksigner verify` 验签并复制 `mods/bepinex/dist/mystia-steward-companion-android-arm64-v8a.apk` 和 `mods/bepinex/dist/mystia-steward-companion-android-armeabi-v7a.apk`；`build-release.ps1 -BuildAndroidApk` 和 `publish-release.ps1 -BuildAndroidApk` 只是复用该签名构建流程，不允许把 keystore、密码和签名配置提交。Android Gradle 已关闭 Kotlin incremental compilation，避免 Windows 上 Cargo registry 与项目分属不同盘符时出现 Kotlin daemon 相对路径报错。APK 需要 Android 工具链构建、签名和真机验证，作为独立 Release 资产上传，不参与 Mod 自动更新。
- Android APK 体积优化只允许在 Android 构建脚本中通过 `CARGO_PROFILE_RELEASE_*` 环境变量启用；不要把 `lto` 或 `codegen-units` 写入全局 Cargo release profile，避免普通 Windows `build-release.ps1` 构建被 Android 优化拖慢。
- `apps/companion/src-tauri/Cargo.toml` 的 mobile lib target 使用内部名 `mystia_steward_companion_mobile`，不要改回 `mystia_steward_companion`。Windows MSVC 会把桌面 bin target `mystia-steward-companion` 的 PDB 名规格化为 `mystia_steward_companion.pdb`；同名 lib target 会触发 Cargo `output filename collision` 警告。

## 运行时约束

- 血池地狱的厨具拓扑观察器只 Hook BepInEx 783 中唯一命中的 public declared-only `EventManager.LockCookers(...)`、`LockCookers_Forever(...)` 和 `PartnerManager.OnCookerAvailabilityUpdate(int)`。Prefix/Postfix 只在方法同步执行期间维护 mutation depth，并在 `__runOriginal=true` 正常返回时推进 topology revision；不得枚举或改写传入 `IEnumerable<int>`、out Action、结束回调或 `LockedCookersRaw`，也不得 Hook 状态机、闭包或 `MoveNext`。永久锁锅返回后不能因为 `LockedCookers` 非空保持全局屏障；只有完整 Hook、当前血池经营 generation、无活动 mutation，以及 fresh `AllCookers + LockedCookers` 的规范 SHA-256、控制器数量和锁定数量同时一致时才签发/复核短租约。任一条件失败只阻断血池地狱厨具副作用，普通经营和幽幽子不进入该 tracker。
- Mod 只读取当前游戏运行时数据，不读取 `.memory` 存档文件。
- 运行时固定数据读取成功后，C# 侧会把 `DataBaseCore` / `DataBaseCharacter` / `DataBaseLanguage` 结构化为 `RuntimeDataCatalog`，并切换 `DataRepository` 到运行时仓库；伴随窗口收到 `/snapshot` 中的 `runtimeDataComplete=true` 和新的 `runtimeDataSignature` 后，只在本地缓存为空或签名变化时读取 `/runtime-data`，普客/稀客推荐、经营中推荐、库存修改页和自动化目标解析都必须使用这份缓存后的运行时数据集。核心目录 ID 只能枚举 `DataBaseCore.IngredientsMapping`、`BeveragesMapping`、`FoodsMapping`、`RecipesMapping` 和 `IzakayasMapping` 五张精确 `Dictionary<int,string>`，再逐 ID 调用对应 `Ref*`；不得恢复 `GetAll*()` 目录枚举或静态 fallback。五张 mapping 的键和值都必须先严格验证为 CLR `Int32` 和非空 CLR `String`，并验证原始 ID 唯一性；其中材料、酒水、料理和配方使用非负内容 ID 域，在唯一投影边界排除负数内部键且不得调用对应 `Ref*`，排除后为空时整轮 fail-closed。`IzakayasMapping` 使用完整 signed ID 域，不把四类内容对象的规则外推到地点；允许 signed ID 的料理/酒水 Tag 字典也保持独立规则。处理 `IzakayasMapping` 时先精确读取 `DaySceneMapLabel`，只有空标签占位条目允许跳过；非空标签必须再精确读取 `DaySceneMapName`，名称读取失败令整轮 fail-closed，合法但不属于项目支持日间经营地点的条目才记录 skipped。确认支持地点后再严格读取普通/稀客池，不读取特殊经营和占位条目中与推荐无关的合法空池。基础稀客只从 `GetAllSpecialGuests()` 精确引用数组读取声明的 raw fields；映射稀客只读取 `GetAllMappedGuests()` 与 `GetAllSpecialGuests()` 的 ID/StringId/source chain，不生成 dummy、临时稀客或调用喜好/厌恶计算属性。identity 快照必须保留全部基础与映射原生身份，即使其中部分无法映射为本地可推荐稀客；核心目录与 identity 独立记录完成状态，二者均完整前不得构造推荐状态 provider。目录重试只由控制器的单一有界时钟调度，失败必须发布带阶段的精确状态，不能再进入 provider 后用泛化的目录不完整异常覆盖。普通地图或就绪变化复用已完成的静态身份快照，只让未完成读取立即重试；进入主菜单、存档运行态被清空或 identity 不完整时必须独立重建，不能因核心目录仍完整而跳过。
- 伴随窗口要把 `/runtime-data.rareCustomers` 投影为两个用途单一的数据域：普通页面和常规推荐使用的 `rareCustomers` 必须有合法日间地点；特殊经营评价使用的 `rareCustomerProfiles` 只要求 canonical ID、有效名称及完整料理喜好/厌恶和酒水 Tag，不得因 `places=[]` 删除。特殊经营只能按已验证的 canonical character ID 读取档案，不允许映射 ID、名称或静态表回退；两个投影和 NormalOrder 特殊目标缓存都必须绑定同一推荐数据签名。
- 本地 API 快照需要避免在 Unity 主线程和 WebView IPC 热路径高频序列化大对象。完整 `RuntimeDataCatalog` 不得再随 `/snapshot` 周期性发布；`/snapshot` 只携带运行时目录完成状态、来源、状态文本和签名，完整目录由 `/runtime-data` 独立端点按签名懒加载。`runtimeDataSignature` 必须对将要发布的完整 `/runtime-data` JSON 计算固定 64 字符 SHA-256，不能只拼集合数量或首尾条目；目录对象代际未变化时复用同一 JSON 与签名，避免每个快照周期重新序列化。`runtimeDataComplete` 与 `runtimeDataStatus` 必须使用 core+identity 组合门禁，不能在 identity 失败时把已序列化的 core 目录误报为可消费。旧的 `/snapshot.runtimeRareCustomers` 和合成 `RuntimeRareCustomer` 模型已删除，可推荐稀客目录唯一来自 `/runtime-data.rareCustomers`。`/snapshot` 必须携带 `snapshotSignature`，该值必须是规范快照内容的固定长度加密摘要，不能直接使用会随订单和事件增长的规范原文；前端轮询时带上 `knownSignature`。内容未变化时后端返回 `{ unchanged: true, snapshotSignature }`，前端只更新连接存活状态，不得重新 `setSnapshot`、重新派生推荐或触发 worker。伴随窗口收到完整目录后应保存到独立缓存，不得放进主 `snapshot` state，避免大对象随轮询快照、Tauri IPC 字符串、props 链路和 React DevTools/闭包长期留存。前端只锁存已完整目录来抵抗瞬时失败；未完成的占位目录必须随最新快照更新来源和失败状态，不得长期显示首次“等待中”文本。快照内容签名未变化时后端应复用上一份缓存 JSON，不要为了 `CapturedAtUtc` 或性能数字重复序列化。前端仍必须按成功返回的快照更新连接状态，避免跳过运行时目录恢复。运行时固定数据读取完成后不得在经营快照热路径重复刷新，未完成时也要做重试间隔保护；经营诊断只能复用已缓存的运行时目录快照。聚合快照诊断必须包含 `runtimeSceneReadiness`、`runtimeDataComplete`、`runtimeDataSource` 和 `runtimeDataStatus`，让日间目录失败不依赖夜间手动诊断。新增重扫描或自动化轮询时要记录到 `performanceMs` 或复用现有耗时指标，便于概览页排查掉帧；性能快照只保留近期样本，避免旧耗时长期误导判断。经营扫描指标应尽量按来源拆分，例如 `business.rare.*`、`business.normal.*` 和 `runtime.cookerSnapshot`；普客订单快照应优先复用短 TTL 缓存，不要在一次快照发布链路中重复枚举同一批运行时对象。
- 夜间经营订单只允许在 Work 场景且精确经营生命周期处于 Active generation 时读取。稀客正式订单集合只接受当前 generation 已由完整生命周期 Hook 覆盖的 `SpecialOrderRuntimeCapture`；捕获已就绪但结果为空时，空集合就是权威业务结果，不得从 `OrderController`、HUD、服务面板、桌位控制器或 `GuestsManager` 补造订单。诊断开启时可以额外采样这些来源，但诊断样本只能进入日志快照；控制器扫描仍可读取活动稀客和预算资金信息。七 Hook 与当前 generation 就绪后，`NormalOrderRuntimeCapture` 的精确普客捕获是业务和执行权威，即使单轮 `OrderController` / HUD 读取为空或失败也不得过滤该捕获；HUD 只补充当前可见但没有捕获绑定的不可执行行。同一非零 native order key 的 HUD 与捕获记录必须去重为一笔捕获订单，不能按桌位、料理或酒水重绑。
- 夜间经营稀客订单必须保留订单级 `IsFreeOrder`。免费订单不应用 `GuestGroupController.WillPayMoney=false` 的付款预算阻止；非免费订单继续按当前预算策略和剩余资金判断。
- 夜间经营订单必须按首次出现时间稳定显示；不得因桌号排序或推荐完整度排序让新订单插到旧订单前面。
- `经营中 -> 推荐` 的稀客与普客页签必须共用 `当前订单方案` 展示框架，但不得合并两类数据语义：稀客显示点单主执行方案和候选料理/酒水，普客显示游戏原订单与唯一当前执行方案。两类空状态都必须区分尚无订单、计算中、计算失败和确定无结果，不得用统一的“暂无推荐”掩盖运行时或 Worker 失败。
- 经营中订单排序支持 `点单顺序` 和 `稀客分组`。默认必须保持点单顺序；稀客分组模式下，同一稀客订单放在一起，稀客组之间按该稀客最早订单出现时间排序，组内仍按点单先后排序。经营中列表、当前订单方案、专注模式、游戏界面置顶目标和自动化第一单选择必须复用同一排序函数。特殊经营可以在订单层跳过没有主执行计划的订单，但选中订单后页面、自动化和游戏界面辅助只能消费该订单的 `executionPlans[0]`。普客特殊经营自动化目标选择是候选搜索级工作，必须通过经营中订单推荐 Worker 预计算并按订单 key 复用，自动化 tick 只能消费结果，不能在 UI 主线程逐单同步调用完整目标搜索。主推荐 Worker 的输入签名只包含算法实际消费的语义字段，`lastSeenAtUtc`、诊断来源和显示标签不得触发重算；页面可按当前订单强身份暂存同一硬上下文中的上一轮结果，新订单局部显示 pending，送达状态或需求变化立即失效。硬上下文至少包含连接代际、自动化会话 ID、经营 generation/lifecycle、特殊经营推荐语义和数据签名；展示缓存不得进入自动化、置顶或高亮，动作路径继续依赖 Worker 的 current/pending 门禁。
- 稀客/经营中推荐使用统一料理/酒水列表：满足点单 Tag 的候选优先，不满足点单但命中稀客偏好的候选直接进入同一列表并标注 `偏好备选`。不得再维护满足点单与喜好备选两套结果数组。自定义推荐料理必须从 `custom-recipes.json` 转换为普通料理候选参与同一套硬过滤、预算、排序、自动化和 UI 展示，不得再通过 `favorites.source=manual` 或其他兼容路径混入收藏体系。`custom-recipes.enabled` 是唯一功能总门禁，关闭时不得改写单条 enabled、pinToTop 或 sortOrder；管理页的稀客/基础料理分组只能从同一列表派生，flags 批量更新必须由 Mod 在一个锁和一次保存内原子完成，排序只允许在同一稀客内移动。自定义推荐料理的单条置顶、收藏料理和收藏酒水分别按独立规则置顶。置顶不得绕过解锁、库存、预算阻止、排除项和缺失厨具过滤。料理推荐优先 `foodScore >= 3`，但必须保留“满足点单且低于 3 分”的候选作为兜底。
- `v1.2.x` 只保留 Local API 启动阶段执行的 `favorites.source=manual` 到 `custom-recipes.json` 一次性数据迁移。必须先原子写入目标，再删除来源，并通过当前自定义料理身份键去重，使中断后可重试且不丢数据。迁移不得延迟到 `GET /custom-recipes`，只读和 CRUD 端点不能隐含迁移副作用。该代码在 `v1.3.0` 删除；旧 GUID 配置、旧 API 路径、旧类型和旧业务逻辑不得借迁移名义重新引入。
- 稀客页场景候选必须优先使用运行时数据集；只按经营场景、可用中文名称和可用点单 Tag 过滤，不再按当前存档记录/解锁进度过滤。当前进度来源不稳定，容易误删可测试稀客；后续若要恢复，必须先基于游戏运行时明确的已解锁字段重新实现。
- 物理厨具快照只允许 complete 和 unavailable。每轮先读取 `EventManager.get_LockedCookers()` 的精确坐标，再将该集合传入 `AllCookers` 读取；命中锁定坐标的字典条目只校验 key、native pointer 和唯一性后计入 `placedCookerLockedControllerCount`，不调用可能已失效的 controller `GridPosition`、`CouldOpen`、内容、能力或渲染器 getter，也不进入 `placedCookers`、类型投影、推荐、高亮或自动化容量。非锁定条目仍必须确认 controller native pointer 和 grid position 各自唯一、字典 key 与 controller position 精确相等且 `CouldOpen=true`；任一读取失败或矛盾都使整轮 unavailable。`AllCookers` 中未摆放厨具的固定控制器必须同时满足精确 `IsEmptyDesk=true`、`AllAvailableCookerType` 为 Empty-only、空闲 phase 且无结果/配方，才计入 `placedCookerEmptyControllerCount`；它保留原始 controller index，但同样不进入业务投影。完整 wire 必须满足 `placedCookers.length + placedCookerEmptyControllerCount + placedCookerLockedControllerCount == placedCookerControllerCount` 且读取失败为零；unavailable wire 不得发布 placed/type/empty，仅可保留安全分类的 locked 诊断数量，并满足 `placedCookerLockedControllerCount + placedCookerReadFailureCount == placedCookerControllerCount`。unavailable 不作为厨具过滤或锁定类型推断证据，展示推荐保持既有语义，但自动化容量、厨具写操作和高亮必须 fail-closed。只有 complete 快照可让 `排除缺失厨具` 把未出现类型作为硬过滤证据；complete 且 locked 计数大于零时，只有存活非锁定条目证明的类型保持可用，其他类型作为独立运行时硬门禁且不受该开关影响，恢复后随快照签名重算。推荐可开类型只用于候选硬门禁，自动化实体容量继续使用后端更严格的 `automationAvailable` 分类。料理厨具类型以游戏 `CookSystemManager` 的 `AllAvailableCookerType` 为准，前端只消费本地 API 给出的中文厨具名。设置页的 `同基础料理显示` 控制同一基础料理在稀客页和经营中推荐中最多展示多少个加料变体；该限制只裁剪主计划之外的 UI 展示行。经营中必须先从完整料理/酒水候选构造并排序执行计划，以 `executionPlans[0]` 作为唯一主执行计划，再把主计划料理和酒水投影为页面首项。自动化开始、游戏界面置顶、制作页加料料理选项、料理/材料/酒水列表项高亮、目标厨具高亮、目标桌位高亮和目标订单高亮都只读取这一个主计划，不得扫描隐藏的后续计划；加料选项只能提交所属主计划的有序加料。自动化收藏限定只有在总开关、对应料理/酒水阶段和当前订单自动化权限都开启时才参与主计划归一化，并且必须在计划数量截断前处理；没有匹配计划时不执行对应自动化动作，不得建立兼容性旁路。非可见经营中页的后台推荐只能返回业务需要的紧凑结果，并限制 worker 候选缓存和回包重复更新。
- 夜间 live 厨具控制器只读取 `CookSystemManager.get_AllCookers` 的精确 `Dictionary<Vector3Int,CookController>`，并通过 `RuntimeConcreteCollectionReader.TryReadDictionary` 得到完整 key/value 条目；开锅、厨具快照和高亮共用该入口。每个控制器的完整能力只接受 `Cooker.AllAvailableCookerType` 的精确 `IEnumerable<CookerType>`，在 BepInEx 783 下分别使用泛型 `Current`、非泛型 `MoveNext` 和 `IDisposable.Dispose`，不得退回基础 `Cooker.Type`。序列必须至少包含一个合法枚举值；只有精确 `IsEmptyDesk=true` 才接受 Empty-only，非空控制器必须至少包含一个 1-5 的能力。挑战锁定只读取直接泛型基类 `MonoSingleton<EventManager>.get_Instance()` 和精确 `EventManager.get_LockedCookers()` 返回的 `Il2CppStructArray<Vector3Int>`，按 Length/indexer 读取，不使用 `LockedCookersRaw`。不得用通用单例、`AllCookerControllers` object 枚举、字典内部 slots 或 Unity 场景扫描补齐。快照与实际开锅必须共用后端对严格空闲、已完成 `Extract` 残留和不可用状态的完整分类；`CouldOpen=true` 单独不能生成自动化容量。前端必须校验快照计数、规范 `controllerIdentity`、唯一 `gridPosition`、`challengeLocked`、类型投影和分类字段的一致性，再按 `controllerIndex + controllerIdentity + gridPosition` 建立实体槽位；一台多类型控制器同一轮只能预约一次，不可用轮次容量为零。料理写请求必须携带本轮三重预约；Mod fresh-read 同一份 `AllCookers` 与 `LockedCookers` 后复核索引、native pointer、坐标、类型和状态，并在紧邻 `SetCook` 前再次复核。任何漂移都局部等待最新快照，不得改选其他兼容厨具。普客与稀客共用同一槽位表，需要开锅的候选只有通过厨具准入后才消耗并发；队头订单等待某类厨具时必须继续检查后续可使用其他类型的候选。已有厨具处于自动化预约、游戏忙碌或挑战暂时禁用状态时应返回可重试等待和分项计数，不能报告为厨具集合为空。
- 稀客推荐只有在最终 `executionPlans` 为空时才构造 `blockedDiagnostic`。分阶段计数必须复用正式候选生成、特殊经营过滤和计划构造入口，按料理目录、点单 Tag、解锁、基础材料、厨具、特殊规则/厌恶安全、酒水库存/排除/Tag、预算、特殊评价和最终计划定位首个清零阶段；允许附带缺失材料、所需/已摆厨具、剩余预算和最低组合价。该结构只解释现有结果，不得把被过滤候选放回列表、改变排序或降低特殊经营条件。幽幽子第二阶段没有可预测 `ExGood` 的完整组合时必须保持无计划。自动化诊断用稳定状态签名在当前会话内做有界去重，切换会话清空；诊断签名不得参与推荐缓存身份或目标选择。
- 游戏列表置顶、制作页加料料理选项、厨具高亮、桌位高亮和订单高亮按 rare/normal 分别设置，但共用一个双槽 target set。伴随窗口只通过 `POST /ui-pinning/targets` 原子发布完整集合；顶层只携带当前正数 `businessGeneration` 和 `targetCount`，每个 indexed target 携带自己的 `ListPinningEnabled / RecipeVariantEnabled / CookerHighlightEnabled / SeatHighlightEnabled / OrderHighlightEnabled`、六位大写 RGB、0-based `deskCode`、正式 `targetRevision`、类型匹配的 opaque `R-*`/`N-*` trace、正数 lifecycle 和完整组件。normal 另须携带原始非零 `ptr:` order key，`recipeId` 使用游戏 `Recipe.Id`。功能全关的类型不发布，`targetCount=0` 清空；旧集合级开关、旧 singular 路由和未知 query 字段必须拒绝，不迁移、不忽略。目标只保留尚未送达的组件，一槽结束、失败或身份失效不得清除另一槽。两槽各自保持 current/pending/error 状态；发布器仍是单写者，硬上下文变化必须先成功发布空集合，再重发当前目标，迟到请求不能恢复旧集合。Mod 只接受当前 Active generation，并以 active capture、lifecycle 和 native order/controller 身份唯一匹配。
- 制作页加料选项只为 `RecipeVariantEnabled=true` 且 extras 非空的目标生成。相同 recipe ID 和 ordered extras 合并 claims，不同 extras 分别建立独立 native Recipe 行；基础行始终保留原配方。某方案在当前厨具的 fresh 原生列表中命中 `0` 个权威 Recipe 表示不适用，直接跳过；精确命中 `1` 个才生成，超过 `1` 个 fail-closed。选择加料行时，transaction identity 同时绑定权威与 source synthetic native Recipe pointer，并保留不可变 origin identity；后续 target snapshot 只能更新 recipe surface attachment，不能改写 origin。正常基础行不应用 extras。Recipe 行池化复用只交接 Mod exact lease，不清原生 callback、不写 `interactable`；只有 fresh 证明仍属于旧 Mod output closure 时，才能清理旧 output ownership。不得按名称、文本、路径、索引或 recipe ID 兜底。
- 加料事务须复核 panel/button/recipe pointer、经营 generation、target revision、基础材料、五格上限、自由烹饪、倍率和库存，并在同一 target publication lock 内完成一次 `IngredientOutRange` 和一次选中材料 List 写入。列表 Insert 必须在 512 项上限内逐行 readback；surface 读取或插入失败只退休本轮料理列表绑定，不锁存 mutation uncertainty。只有 Insert 已调用但结果无法确认时，才按 business + target generation 写入 ledger 并禁止重放。非 final 手动关闭在 `Applied/OutputPending/OutputReady` 后正常返回才能以原生退料为收据记 `Cancelled`；`Applying` 中关闭或关闭异常记 `Uncertain`。库存副作用一旦无法确认，当前页面停止继续选菜，加料选项只在新的经营 generation 清除锁存。`OnOutputSelected` prefix 不清当前 callback；只有原生正常返回后才验证 fresh 安装的 raw MethodInfo pointer、Target、panel、output 和 solved，或 post-clean 本次非目标候选。最终闭包必须持有同一 business generation publication lease，不依赖 origin target snapshot 仍是当前引用；只有原闭包、同步 `OnPanelClose` exact receipt 和 retained `OutputSubmitting` sequence 全部成立才记 `Completed`。`SetCook` 及后续动作只由游戏原闭包执行，任何副作用不确定都不得自动重试、猜测回滚或换用其他方案。
- 游戏运行时视觉服务只允许后台发布 immutable desired target；Unity 对象的读写、恢复和销毁必须在明确的主线程边界执行。源对象、目标和池化身份必须精确且唯一；证据不完整或形态漂移时 fail-closed，不得按名称、文本、路径、列表位置或容差猜测。Mod 只修改自有视觉对象；Closing 清理仍存活的对象，Destroyed 只遗弃 wrapper。不保留旧 Hook、备用视觉或兼容路径。具体入口、身份和 ownership 契约见 [`mods/bepinex/README.dev.md`](../mods/bepinex/README.dev.md)；回归边界由 `tests/ui-pinning-runtime`、`tests/runtime-order-highlight`、`tests/runtime-seat-highlight` 和 `tests/runtime-throw-delivery-order-highlight` 固定。
- 夜间经营生命周期只能由一套精确状态机拥有。`WorkSceneSustainedPannel.OnPannelPostOpen/0`、`GuestsManager.CloseIzakayaDelayed/2`、`CloseIzakayaAndLeaveChallengeMode/2`、`NightScene.SceneManager.ToResult/0` 和 `OnInstanceDestroyed/0` 五个 Hook 必须全部成功才允许 Active，缺少任一成员都 fail-closed。`TryCloseIzakaya` 只表示倒计时结束、停止接客并等待在座顾客完成服务，不是 teardown Hook；此清桌期间保持同一 Active generation，订单捕获、推荐、自动化和界面目标继续工作。工作面板 post-open 从 Inactive/Destroyed 开启递增 generation；延迟结算、特殊经营退出或结果页转换才进入 Closing，立即阻止新运行时访问、失效目标并清理订单捕获、特殊上下文、料理 generation 和自动料理 job；实例销毁进入 Destroyed。Closing 不能重新 Active，下一场只能在 Destroyed 后开启新 generation。所有跨帧命令和非幂等原生调用必须捕获 generation，并在每个可能触发重入结算的原生调用后复核；generation 或 phase 改变时立即停止后续游戏对象访问。
- 稀客料理/酒水展示排序由伴随窗口设置页的置顶开关、自定义推荐料理置顶和推荐权重共同控制。顺序固定为硬过滤、任务料理置顶、自定义推荐料理置顶、收藏料理/酒水置顶，最后再由 `RecommendationSortProfile` 的启用项、权重、方向和预设决定普通顺序；默认均衡预设综合稀客偏好、厌恶风险、加料数量、资源压力、成本、利润、酒水库存和当前厨具可做。新增排序或置顶规则时必须同时接入稀客页、经营中页、专注模式、缓存签名和唯一主执行计划，不要让页面首项、自动化初始锁和游戏界面辅助出现不同目标。
- 推荐 tag 解析统一维护在 `apps/companion/src/recommendation-engine/tag-resolution.ts`，动态料理 tag 维护在 `dynamic-food-tags.ts`。运行时导出的 `tagPriorityRules` 优先级最高；运行时缺失时只允许使用 `PROJECT_VERIFIED_TAG_PRIORITY_RULES` 这组项目验证规则，不得在其他模块重新硬编码互斥/压制关系。新增或调整 tag 规则必须来自游戏运行时行为、反编译资料或可复现实测，并同步更新该集中模块和相关文档。
- 运行时料理配方的基础食材必须作为数量敏感序列处理，重复项表示同一材料需要多份；`RuntimeDataCatalog.Recipes[].Ingredients` 和前端 `RecipeCatalogItem.ingredients` 不得去重。只有 tag、场景、ID 列表等集合语义字段可以在解析和归一化时去重。推荐加料槽位、大份动态 tag、基础成本和自动化下单保护都必须基于保留重复后的真实基础食材数量。
- 普客与稀客捕获只有在七个精确生命周期 Hook `PushToOrder`、`SetManualControllerOrderInternal`、`RemoveFromOrder`、`EvaluateOrder`、`EvaulateManualOrder`、`CleanOrderInfo`、`RepellInternal` 全部安装，且当前经营 generation 从开始时已被完整覆盖后，才可进入业务投影和自动化。若 Hook 在经营中途才补齐，本场保持 fail-closed，下一经营 generation 才能就绪。订单只能由成功返回的创建 Hook 建立精确 order/controller/native-key 绑定；native key 必须来自非零 IL2CPP 原生对象指针，读取不到时拒绝捕获，不得用 managed hash 或其他派生值兜底。生成、HUD、伙伴状态和当前稀客身份不能补造所有权，也不使用短时间宽限。`EvaluateOrder` / `EvaulateManualOrder` 只在调用前订单已 `IsFullfilled=true` 且原方法成功返回时退休，`RemoveFromOrder` / `CleanOrderInfo` 在原方法成功返回后退休；成功返回的 `RepellInternal` 无论 `out haveSeated` 为何值都退休调用前捕获的订单。`EndDlc4SpecialManualOrder` 不是订单退出边界。动作前需要复核时只读取 `PeekOrders()` 当前栈顶。`AllOrders` / `AllOrdersData` 是只累积历史订单的栈，不得解释为活动订单集合。
- 本地 API 必须始终保留 `127.0.0.1` 回环监听，避免 LAN 配置错误导致用户无法从 A 设备本机恢复；`LocalApi.AllowLanConnections=true` 只会额外启用 LAN listener。LAN listener 必须限制私网来源，连接配置和 Token 重置端点只能由回环客户端调用；每个 listener 必须拥有独立停止状态和线程所有权，动态重配要串行、幂等并等待旧 worker 有界退出，accept 终止错误不得无延迟重试或逐条刷日志。Tauri 代理只接受 loopback/private/link-local IPv4 endpoint，除 `/health` 外所有接口都必须通过伴随窗口传入的 token 访问。
- 本地 API 路由只接受规范路径，不提供根路径 `/` 到快照或健康检查的映射，也不接受 `/api/*` 前缀别名。只读查询使用 `GET`；文件写入、运行时修改、配置变更、控制权变更、诊断导出/打开目录和更新操作统一使用 `POST`，即使参数当前仍放在 query 中也不得退回副作用 `GET`。`/updates/status` 会归并 updater 结果并可能写入或删除状态文件，必须使用 `POST`。新增客户端、mock 和文档必须同步方法与精确路径；不存在的路径/方法组合返回 404，GET、POST、OPTIONS 以外的方法返回 405。在正式定义结构化 body schema 前不得自行编造 JSON request body 契约。
- 正式 Tauri runtime 的 Rust TCP 代理必须把连接超时和响应读取超时分开处理：连接超时最长 5 秒，读取超时可按前端命令要求放宽到最多 60 秒。长耗时请求不能被连接上限提前截断；浏览器开发模式的 fetch 超时仍由前端调用方控制。
- 每个伴随窗口必须生成稳定客户端 ID，并通过本地 API header 传给 Mod。订单自动化端点必须由 Mod 本地 API 的 automation control lease 仲裁，同一时间只允许一个客户端执行自动化；其他客户端可以读取快照和配置普通偏好，但不得绕过 automation control lease 直接调用订单自动化动作。连接断开或快照错误时，前端可以保留最近一次只读展示，但不得继续用旧快照驱动自动化、游戏界面置顶目标或其他写入游戏运行时的后台动作。断线重连只允许一条退避调度路径重试带 Token 的 `/snapshot`；`/health` 只能用于人工可达性诊断，不能清除快照错误或恢复已连接状态。相同 endpoint/token 的 Tauri 启动连接通知必须幂等；automation control lease 的连接身份由 endpoint、token 和 Mod `automationSessionId` 确定，短暂网络错误不能伪造新服务端会话。桌面和 Android 等所有正式 Tauri runtime 必须统一通过在线程池执行阻塞 TCP 请求的 Rust command 访问本地 API，只有浏览器开发模式可以直接 `fetch` mock API，不得保留移动端 WebView 网络 fallback。
- 伴随窗口单实例控制监听 `127.0.0.1:32146`；Tauri 必须在初始化窗口前原子绑定端口，再把预绑定 listener 移交控制线程，绑定失败的并发实例只通知端口所有者并退出。热键先发送 `show`/`toggle`/`exit`；控制端口不可达时进入 single-flight launch，所有权保持到新进程端口可连接或明确超时，不得用固定时间节流代替实例所有权。
- `F8` 和 `RS Click` 默认用于在游戏和伴随窗口之间切换焦点。默认 `RS Click` 同时读取 legacy `JoystickButton9` 与 Input System `Gamepad.current.rightStickButton`，物理释放后才重新武装；只有默认 `JoystickButton9` 使用 Input System 补充，自定义键仍以配置的 legacy `KeyCode` 为准。所有 `toggle` 来源共用 Tauri 唯一的 in-flight/cooldown gate；只有实际聚焦或显示完成才提交冷却，失败返回结构化状态。Windows 聚焦成功以 `SetForegroundWindow` 非零返回值为首要证据；返回零时仅当前前台窗口属于目标进程可视为幂等成功，不得要求激活转换期的前台 HWND 与枚举 HWND 立即精确相等。切回游戏时必须先成功聚焦游戏，再按设置隐藏窗口。
- 伴随窗口透明度通过 Tauri transparent window 和前端 CSS 变量实现；背景透明度只影响窗口背景、面板、弹层和滚动条轨道，文字透明度只影响普通文字、图标和辅助徽章内容，主操作按钮必须保持可读。字体大小由 `fontScalePercent` 和单一 `--companion-font-scale` 驱动，范围固定为 90% 至 130%、默认 100%；Tailwind、Mantine、自定义控件和继承文本必须共用该比例，固定间距、图标、窗口几何和响应式断点不得随字号缩放。不要使用 CSS/WebView zoom、transform 或逐组件覆盖实现字号。不要用 Windows `SetLayeredWindowAttributes(..., LWA_ALPHA)` 或其他整窗 alpha 实现背景透明度，因为它会让文字和图标一起变淡。桌面窗口状态统一由官方 `tauri-plugin-window-state` 管理，只持久化普通窗口位置、大小和最大化状态；不得持久化可见性、最小化、装饰或全屏状态，避免关闭到托盘或临时最小化影响下次启动。主窗口在 Tauri 配置中保持初始隐藏，由插件先恢复有效显示器内的普通边界和最大化状态，再由 Rust setup `show()` 并聚焦，避免默认位置闪烁。全平台图标的唯一母版是 `apps/companion/src-tauri/icon-source.svg`；母版必须使用 LF、正方形 viewBox、自包含纯矢量，并在根元素分别声明通用 `data-icon-background="#RRGGBB"` 和 Android adaptive `data-android-background="#RRGGBB"`，同时保留唯一的 `icon-background` 和 `icon-foreground` 分层。修改后运行 `pnpm icons:generate`，提交桌面 PNG、Windows tile、ICO、tray、ICNS、iOS、`icons/android`、APK 实际使用的 `gen/android/app/src/main/res` 图标和 favicon，再用 `pnpm icons:check` 做全量幂等、尺寸、alpha、安全区和结构校验。Windows ICO 必须保留 16/20/24/32/40/48/64/128/256 九层且将 256px 置于第一层；ICNS chunk 固定排序；iOS 由同背景色的不透明全幅派生图生成；Android legacy 使用规范 padding 和 48/72/96/144/192 尺寸，adaptive foreground 只包含缩入官方安全区并保留额外桌面视觉留白的 `icon-foreground`，不得退回整张预圆角图或保留旧移动端资源。
- 鼠标穿透锁定必须通过 Tauri 原生窗口 `set_ignore_cursor_events` 实现，不能只使用 CSS `pointer-events`。`F10` 负责切换鼠标穿透；`F8`、`RS Click`、托盘显示/重连和单实例 `show` 控制消息必须自动关闭穿透，避免窗口被唤回后仍无法点击。
- 伴随窗口根滚动区域必须预留稳定纵向滚动条槽位，避免页面内容因滚动条出现或消失产生横向跳动。
- 伴随窗口滚动条样式必须跟随主题和背景透明度；不要使用全局 `*::-webkit-scrollbar` 覆盖会刻意隐藏滚动条的导航栏。
- 前端输入采样与 DOM 焦点必须分层。只接受 Gamepad API `standard` 映射；按钮按 `pressed || value > 0.5` 判断，摇杆使用 `0.65/0.40` 按下/释放滞回，十字键优先且斜向每帧只产生一个主方向；首次重复为 360ms，后续为 140ms。初始、focus/visibility 恢复、重连、设备替换和重新启用导航均要求至少 2 帧且连续 50ms 中立，`RS Click` 不受窗口内手柄导航开关影响。
- 伴随窗口内手柄焦点只纳入可见、可交互元素，并优先遵守 `data-gamepad-scope`。modal 限定导航作用域；`B` 依次关闭 combobox/modal、返回内部页签、返回顶部页签或退出专注模式。局部滚动区必须显式声明 `data-gamepad-scroll-region` 和稳定 key；组件通过 `data-gamepad-control` 声明 number input、select、multi-select、slider、segmented control、tabs、accordion、tree 和 dialog 语义。动态回焦使用稳定 focus/row key、action epoch、`MutationObserver` 与 `requestAnimationFrame`，不得恢复固定 `0/120/320ms` 定时器。若按钮展开的只读滚动区因空间几何不能稳定成为下一候选，应使用声明式 `data-gamepad-confirm-focus-key` 把手柄确认后的焦点交给稳定目标；不得通过页面专用定时器抢焦点，鼠标点击行为也不得被连带改变。通用空间导航按交叉轴对齐选择候选，左右无候选时不得错误滚动页面。
- 经营中、专注模式和日志等实时页面的动态内容区应保留稳定容器和紧凑空状态；不要因为暂无订单、暂无预约或暂无日志就直接卸载整块区域，避免数据刷新时页面大幅跳动。
- 帮助页内容必须保存在 `apps/companion/src/data/help-content.json`，前端只负责搜索、目录树和详情渲染。新增用户可见功能或排查流程时，同步更新帮助 JSON，避免只改 README。
- Unity 场景切换后不要用固定秒数等待规避加载问题。`DaySceneSustainedPannel.OnPannelPostOpen` 只是日间读取的独立最终门闩，不能直接标记 ready；manager/Action 链允许在面板前后捕获，避免依赖非契约化的 UI 时序。普通读档从 `DayScene.SceneManager.OnFirstEnterDaySceneFinish` 正常分支捕获同一 manager 的 `RunTimeScheduler.OnEnterDayScene` 外层 Action；只有该 Action 进入匹配的 `DefaultOnFinish` 后才能捕获 `m_HasTriggerOnEnterDaySceneEvent=false` 时的 `OnEnterDaySceneMap` 最终 Action，并只由该最终 Action 的 `DefaultOnFinish` 返回解锁。手动经营返回必须在入口前精确读取 `NightSceneDirector.IsManualWorkSceneSession`；漏 Hook、漏 Action、面板未打开或字段不可读一律 fail-closed。每次读取继续验证同一存活原生 manager、`IsMapSwapping=false`、有效当前地图，并要求 `RunTimeScheduler.isExecuting`、`SceneDirector.IsInEvent`、manager 的 `isExecutingScheduledActions` 与 `UniversalGameManager.IsSwitchScene` 全部为 false。夜间经营准备读取仍以 `PrepNightScene.UI.IzakayaConfigPannel.OnPanelOpen` / `GoToSpecific` 为 ready 信号，并用 `Cleanup_Generated` / `GotoWork` 清理。IL2CPP 集合不得经非泛型 `IEnumerable`、`Dictionary.Values` 或通用递归枚举器读取；确需全量遍历的已知 `Dictionary<K,V>` 非空时必须校验同泛参 Enumerator 与 `KeyValuePair<K,V>` 后直接枚举，`Count=0` 则前后两次确认仍为 0 并在解析 Enumerator 前返回；有界动态字典只按已验证 key 调精确 `TryGetValue`，值列表和数组使用经元素类型校验的 Count/Length + indexer。任一调用失败、数量变化或元素类型不符时丢弃整组结果，降级为状态提示并等待下一轮刷新。
- 运行时静态目录（料理、材料、酒水、普客、稀客、场景）和玩家存档状态（库存、已解锁、流行 Tag、已摆放厨具）必须分层读取。静态目录可用后应立即发布给伴随窗口，让邀请、普客和稀客基础选项可用；玩家存档状态读取失败时只影响库存和推荐可用性，不应阻塞已经满足日间门禁的稀客邀请。基础推荐状态必须以完整静态目录的 ID 闭包逐项调用 `RunTimeStorage.HaveRecipe(int)`、`GetIngredientCountById(int)` 和 `GetBeverageCountById(int)`，不得生成或解析完整存储容器；材料和酒水都只有精确 `-1` 表示无限，低于 `-1` 非法，零库存不发布。没有任何已解锁料理时等待下一轮读取而不是发布空可用料理集合；玩家等级、流行喜好/厌恶 Tag 和明星店开关使用 `RunTimePlayerData` / `RunTimeDayScene` 轻量 getter 读取。夜间经营准备阶段在准备面板 ready 后允许读取这些基础玩家运行态，用于修改页、普客页和稀客页；但仍不得读取 DayScene 快照或稀客邀请。
- 主菜单 `Main Scene` / `MainMenuPannel` 必须在代码中显式视为非游戏场景，不要只依赖 `NonGameplaySceneKeywords` 默认配置，因为用户已有配置文件不会自动补充新关键词。非游戏场景下不得读取运行时静态目录或玩家存档状态，避免 DataBase/Language 初始化不完整时触发 Unity 空引用。
- 稀客邀请和当前地图读取必须先通过最终地图 Action 状态机，再依赖 `DayScene.SceneManager`、`RunTimeDayScene` 和 `DataBaseDay` 的运行态对象。门禁开放后仍需确认同一 manager、非换图、无待处理入场事件、有效地图，以及 Scheduler、SceneDirector、manager scheduled actions 和全局换场景四项执行状态全部为 false；若任一证据不可验证，外层保持等待并在场景就绪诊断中说明阶段。
- 结构化任务业务分成两条互不替代的边界：已接取任务继续从 active tracking 状态发布；available 只发布已由新版反编译资料与现有实机日志交叉确认的 `OnEnterDaySceneMap=0`、`OnEnterDayScene=1` 和 `KizunaCheckPoint=5`，来源可为精确 `postMissions` 或 `postMissionsAfterPerformance`。前两类标记为 automatic，羁绊检查点标记为 conditional，原生事件已完成但 after-performance `StartMission` 尚未得出结果时标记为 triggering。`SaveManagement.TryUpgradeSaveVersion(PlayerSaveFile)` 成功返回后，可在同一 Unity 主线程对该返回对象调用一次 `GenerateSaveString(..., Formatting.None)`；原始 JSON 不得落盘、写日志或跨阶段保留，只能在 32 MiB、64 个 DLC 分区、每分区 2048 个 bucket、4096 个 tracking 任务、每任务 256 项 finish-state/condition-data（每项 condition-data 最多 256 个值）和 20000 个 finished 标签的硬上限内解析为托管种子。紧邻的 `RunTimeScheduler.Initialize` 只能对实际 DLC 具体字典读取前后稳定的 `Count`，并对存档 DLC 标签调用同形态 `ContainsKey`；禁止读取 indexer/value 或枚举该 non-blittable 值字典。CORE 与实际入选 DLC 按原生日期偏移规则合并后，必须与 Initialize 返回后的 tracking bucket Count、空 buffer 和 finished 标签多重集一致。type 3 角色对话、当日计划、限时任务和其他尚无足够交叉证据的来源不得由窄入口拼接成业务状态，只保留诊断证据。
- 存档 bool 只作诊断证据；保存的 `conditionFinishStates` 合法为空，也不要求其数量与静态定义条件数一致，不得据此推断或阻断当前进度。每次加载最多同步读取 512 条静态定义，超限即 fail-closed；只缓存反射元数据，不跨 generation 缓存 native 任务或语言对象。静态定义仅允许在 Unity 主线程按 `DataBaseScheduler.TargetNodeExists(label) -> RefMission(label)` 读取精确 `Il2CppReferenceArray<FinishCondition>`、原始 `reciever`、独立 `hasReciever` 诊断值、`conditionType` 和 `amount`；标题只从语言 `Missions` 具体字典精确查键并读取 `LanguageBase.Name`，不得回退 `GetMissionLanguage`、字段或 `ToString()`。`RunTimeScheduler.Initialize` 成功返回后，只按已经验证的 merged bucket 和任务顺序，对 `trackingMissions` 使用已知 int key 精确查值并以具体 List Count/indexer 绑定全部原 `TrackedMissionData`；不得枚举 tracking 字典。首次主动调用前，必须先确认 bucket 数、空 buffer、finished 多重集，并为全部唯一 label 完成上述 `TargetNodeExists -> RefMission` 核心定义预读；任一失败不得调用任何任务对象。每个通过预读的读档任务只主动执行一次 `UpdateFinishStates`，随后必须确认 tracking bucket 数、tracking buffer Count、finished 标签完整序列和逐项任务 label/identity 与刷新前一致，并要求条件状态数量与预读静态定义一致，才原子提交本代任务状态。`GenerateTrackingData` Postfix 只保存该次返回的精确新任务对象；到 `StartMission` 原方法完成、任务已经插入全局列表后，必须在定义预读前、主动刷新后和提交前分别确认任务状态仍为同一所有者线程上的 `Ready + runtimeAvailable` generation；若期间尚未捕获该对象的自然刷新，才主动执行一次 `UpdateFinishStates`，并立即复核 label、identity 和条件数量。任一代际或线程漂移只清理旧 frame，不得调用或提交到新代状态。除此之外不得主动刷新或轮询任务；后续只被动观察游戏自身的自然 `UpdateFinishStates` 和稳定生命周期 Hook。禁止调用 `HasFulfilled`、`ParseActiveMissionData`、任务完成/奖励/移除写入或宽泛枚举。`FinishNodeExtern` 以前后精确 `List<string>` 托管副本验证不变或 append-only，循环任务的 finished 标签重复频次必须保留。每个 Harmony Prefix/Postfix/Finalizer 都必须有最外层 no-throw 壳，finalizer 无条件返回原 `__exception`；inactive 指针转移时同步解除旧身份，active 冲突或迟到旧指针回调 fail-closed。
- `GET /missions/tracked` 只从任务托管状态锁内投影 active-only 列表，公开稳定 label、游戏语言标题、`unverified|tracking|fulfilled`、条件总数与 nullable 条件状态；任一 active 任务的静态定义、标题或已确认的当前条件形态不完整时整轮 `runtimeAvailable=false`，不得跳过坏项后发布部分列表。尚未取得已确认状态的任务按定义条件数发布全 null 状态。`GET /missions/available` 则在每次请求时排队到 Unity 主线程执行一次 fresh read，不能复用冻结诊断报告，也不能由后台定时扫描；排队超时的未开始命令必须取消。available 完整响应以正数 `missionGeneration + sourceRevision` 为身份，每项必须携带有限集合的 `activationMode`、`activationStatus`、`triggerKind`、`sourceTiming` 和 `activationHint`；不得保留旧 `daySceneGeneration` wire 字段或双协议解析。两类响应都只对稳定规范内容生成固定 64 字符小写 SHA-256，`knownSignature` 只压缩返回内容，不能省略 available 的 fresh read 与身份复核。前端 `任务` 页顶层使用 `任务列表 / 稀客邀请`，两个分栏各自提供按当前客户端 `localStorage` 持久化且严格默认关闭的模块总控，不读取旧键、也不因升级自动开启。任务列表只有在总控开启且分栏可见时才读取 tracked/available；关闭或离开分栏必须取消请求、停止轮询、清空页面结果并拒绝迟到响应，重新开启后执行不带旧签名的 fresh read。该开关只控制页面模块，不卸载 Mod 侧共享的被动任务捕获，也不关闭任务料理置顶等独立能力。任务列表内使用 `全部 / 可接取 / 可完成 / 进行中 / 待确认` 受控页签；available 行显示 `自动触发 / 可接取 / 接取中`，同 label 同时出现时 active tracked 项优先，窄屏只允许页签栏水平滚动。source revision 回退必须拒绝；available 与 tracked 任一读取失败都不能伪装成普通空列表。
- tracked 与 available 的每个任务条目都必须发布同一结构的只读展示元数据：原始 `receiverLabel`、游戏语言 `characterName`、静态 `sceneNames` 和非空 `presentationStatus`。无 receiver 明确发布空名称/场景与 `no-receiver`，不得用任务标题或其他角色兜底。普通角色名称只精确查 `DaySceneLanguage.DaySceneNPCLanguage`；稀客名称只能经完整 mapped identity 唯一投影到运行时静态目录的游戏语言名称。相关场景只表示 `DataBaseDay.allNPCs[receiver]` 的静态可能目的地：复核 exact NPC key 后按 BepInEx 783 形态读取 non-blittable `possibleDestinations` 引用数组，以 `DataBaseDay.mapData` 具体字典中 `MapNode.mapSpawnMarkerLabels` 建立唯一 marker 索引，再从 `DaySceneLanguage.MapLanguageData` 精确查键读取场景名。禁止调用 `RefNPC`、`RefDaySceneName`、`GetMapLabelFromSpawnMarker`、枚举 tracked NPC、扫描 Unity 对象或用硬编码名称；展示读取失败只清空未确认字段并发布有界状态，不得让任务状态、可接取资格、ServeInWork 或任务料理置顶失效。缓存只能保存托管值并绑定任务、日间和 mapped identity 代际，不能跨代保留 native 对象。
- `RuntimeScheduledMissionSourceReader` 是 frozen 调度诊断与 fresh available capture 共用的精确只读读取器。frozen 诊断仍要求任务 Ready、日间 readiness、正数 day generation、Unity 所有者线程和完整 `RuntimeMappedGuestCatalogSnapshot`；正式 available 不以日间 readiness/day generation 作为业务门禁，而要求任务 Ready、完整 mapped snapshot，以及同一 mission generation/owner 上已完整安装并武装的被动来源 Hook 与正数 `sourceRevision`。available 开始和提交时复核任务 change version、同一 source revision/owner、同一 mapped snapshot 对象，以及当前日、scheduled 数量、active label 和 finished 历史在读取期间未漂移。只允许对 `RunTimeScheduler.scheduledEvents` 的 `CorrectedDay` 与 `-1` 两个已知 key 使用精确 `ContainsKey/indexer`，事件和 finished 列表使用具体 `List<string>` Count/indexer，任务数组与 `preNodes` 使用精确 `Il2CppStringArray.Length/get_Item`，禁止全量枚举 scheduled 字典或使用 managed array 回退。所有 scheduled/post/active/preNode label 都是非 null、`Length > 0` 的不透明 Ordinal identity；nullable Trigger ID 允许 null、空字符串、纯空白、边缘空白和其他原始字符串，非 null 时只验证类型与容量。上述身份不得 `Trim()`、大小写归一化、重写或建立别名。事件必须先 `TargetNodeExists -> RefEvent`，任务必须按同一原始 label `TargetNodeExists -> RefMission`，并精确读取 `MissionNode.preNodes` 与 `loopedMission`。`finishedEvents`、`finishedMissions` 各有 20000 项上限，采集期间保留空白、重复频次与完整顺序并要求前后逐项相等。`RuntimeScheduledEventDiagnosticCapture` 仍只冻结每个稳定 day generation 的研究证据；正式 available capture 每请求另做 fresh read，绝不从冻结报告投影。
- 被动 available 来源捕获必须在经营/任务读取前一次性精确 Hook public static `ScheduleEvent(string)`、`DismissEvent(string)`、`FinishSchedulerNode(SchedulerNode)` 与 `FinishSchedulerNodePost(SchedulerNode)`；Initialize generation/owner 和 `StartMission` 结果复用既有 `RuntimeMissionDiagnosticCapture`，不得再安装第二套所有权 Hook。四个 Hook 任一缺失、原生异常、finish/start 栈泄漏、direct Start 顺序或数量不符、同 event transition 重复、旧 generation、当前 `postMissionsAfterPerformance` 与捕获数组逐项不一致时，本任务 generation 的 available 来源整代 fail-closed。锁内只保留 event label、来源、原始 ordinal、mission label、有限 phase 和单调 revision，不跨帧保留 IL2CPP wrapper。`ScheduleEvent`/`DismissEvent` 及成功 finish 边界推进 revision；`FinishSchedulerNode` 完成 `postMissions` 的同步 Start 复核后，仅为仍待 performance 的 after-source 建立 transition；`FinishSchedulerNodePost` 必须按相同数组和逐项 `Started|Retired` 结果退休 transition。原生 `StartMission` 成功插入并提交 tracked 后记 Started，确定性无生成结果记 Retired，任何不确定结果禁用本代来源。
- available 业务投影只接受 `OnEnterDaySceneMap=0`、`OnEnterDayScene=1` 与 `KizunaCheckPoint=5`，并接受精确 `postMissions` / `postMissionsAfterPerformance`。type 0 要求 raw Trigger ID 非空但保持不透明，type 1 不伪造 Trigger ID；两者作为 automatic。type 5 eligibility 必须按 IDA 语义纯读重建：`DataBaseDay.allNPCs[raw TriggerId] -> exact NPC key/identity/characterId -> mapped identity Ordinal 唯一匹配 -> characterId == SourceGuestId -> RecordedSpecialNPCs[canonicalId] -> exact CurrentBondLevel/CurrentBondExp`；经验精确等于 L1=6、L2=17、L3=30、L4=45，L4 另要求 `ShouldHaveLevel5KizunaEvent=true`。scheduled 来源只有 eligibility 为 eligible 才能发布；已由 exact Hook 证明处于 waiting-after-performance 的来源发布 triggering，但仍要重读相同事件定义和完整 mission 引用数组。投影还必须重建原生 `StartMission` 门禁：任务当前不在 active labels；所有 `preNodes` 已存在于 finished event/mission history，其中当前 source event 仅按原生先完成事件再启动任务的确定顺序视为本次已满足；`loopedMission=false` 时已出现在 finished missions 的任务必须排除，循环任务也仍需满足其他全部门禁。无 receiver 是合法展示形态；多个来源映射同一任务时仅允许任务元数据完全一致后确定性合并触发分类，冲突整轮 fail-closed。type 3 `OnTalkWithCharacter` 和其他未闭环类型暂不发布为业务结果，仍只保留诊断。禁止调用 `CanContinue`、`StartMission`、`CheckCharacterInteractEvent`、`FillterCharacterInteractCondition`、`HasSpecialNPCKizunaExpFull`、`RefOrGenerateSpecialRunTimeData`、`GetOrGenerateSpecialNPCKizunaLevel`、`RefNPC` 或任何任务推进、生成和触发入口。available 结果只供任务页展示，不得进入任务料理置顶、推荐、自动化、置顶或高亮。
- `RuntimeServeInWorkMissionDiagnosticCapture` 只能被动 Hook 游戏原本调用的 `ContainsSpecialNPCServeInWorkMission`，读取返回值与 `out foodId`，并用已加载 canonical 稀客身份、静态 ServeInWork 定义、任务 generation 和夜间经营 generation 交叉验证；绝不能由 Mod 主动调用。任务 generation 变化、经营 generation 变化或 Closing/Destroyed、Hook/任务读取失败和原生异常必须全量清理旧信号；成功的任务开始、自然状态刷新、完成或移除只能按同一 generation 的最新 active ServeInWork 定义精确复核现有信号，保留 freshness 非 `Fulfilled` 且 receiver 唯一映射到同一 canonical 稀客、food ID 精确匹配的 `(canonicalGuestId, foodId)`，不得因无关任务刷新清空其他稀客。定义、目录、receiver 或 food ID 不完整时复核整轮 fail-closed。普通经营中只有同一 canonical/runtime 稀客恰好匹配一笔未送餐活动订单，且完整目录将 `foodId` 唯一解析为非负 `recipeId` 时，才能向该订单发布同时带 trace、桌位、双重稀客身份和双 generation 的 `MissionRecipePriority`；特殊经营、歧义或不完整证据 fail-closed。前端必须再核对订单身份，并只在默认开启的 `missionRecipePriorityEnabled` 为 true 时从正式候选中精确匹配同一 `foodId + recipeId`。被 exact active ServeInWork 信号确认的任务料理可以跳过普通 food Tag 和料理厌恶判断；配对酒水仍须满足当前酒水点单，料理解锁与库存、材料、厨具、预算、排除项、自动化收藏限定及其他硬门禁均不得绕过。合法任务计划必须排在自定义置顶、收藏料理/酒水和普通权重之前，最终 `executionPlans[0]` 统一供页面、自动化、游戏界面置顶、料理/材料/酒水列表项高亮、目标厨具高亮、目标桌位高亮和目标订单高亮消费。普通经营存在多笔订单时，全局游戏界面目标优先选择主计划为任务计划的订单。关闭设置只从排序上下文移除任务目标，不关闭后端被动捕获；命中主计划显示 `任务目标`，真正写入游戏料理、材料和酒水列表仍由 `gameUiPinningEnabled` 独立控制。已打开列表收到新目标后，只能按目标内容代际在 Unity 主线程执行一次安全刷新；允许 Harmony 只读观察精确 `OnPanelOpen` / close / destroyed 生命周期来登记或清理面板实例，禁止主动调用或重调 `OnPanelOpen` 触发刷新，也禁止场景/面板扫描或轮询。导出包中的 tracked、ServeInWork 与 scheduled-event 三份任务 JSON 仍只用于诊断，另附的 available JSON 是最近一次业务读取快照，四者都不能替代实时 API。禁止 managed/field 兼容读取、复杂 tracking Enumerator、scheduled 字典全量枚举、`AllNodesMapping`、`GetAllNodes()`、`GetAllMissionData()`、`GetTrackedMissionData()`、`ParseActiveMissionData()`、主动 `HasFulfilled`、在上述两个精确刷新边界之外主动 `UpdateFinishStates`、编译器生成 Hook 和窄入口拼接。
- 运行时库存修改必须排队到 Unity 主线程执行，避免本地 API 网络线程直接写游戏对象。
- 运行时库存修改页的材料和酒水列表支持按名称或库存排序；操作只保留单项 `-10`、`+10`、`99`，以及当前存档可编辑材料/酒水批量设为 `99`；不要恢复自定义数量输入、`+1` 或单独“应用”按钮，除非用户明确要求。
- 普客订单自动化必须建立在完整就绪的 `NormalOrderRuntimeCapture` 上。七 Hook 与当前 generation 就绪后，精确捕获是业务和执行权威；`OrderController.GetShowInUIOrders()` 和 HUD `OrderingElement.ActiveOrder` 只补充没有捕获绑定的不可执行可见行，单轮 HUD 空窗或读取错误不得过滤捕获。同一非零 native `orderKey` 的 HUD 与捕获记录必须去重，且捕获绑定优先；不得按桌位、料理、酒水或同桌新订单重绑，不得使用 managed hash 构造 key，也不得在捕获缺失/未就绪时扫描 `GuestsManager`、队列或 `NightSceneDirector.controlledGuest` 创建业务订单。一般普客和稀客动作定位只使用已就绪精确捕获并以 `PeekOrders()` 复核，不再进行反射扫描回退；只有古明地恋 BOSS 与幽幽子三阶段这两个显式命名、各自有额外原生门禁的专用路径可以扫描 manager 当前 controller。普客按首次出现顺序和 `autoNormalConcurrency` 并发调度，订单变化用短防抖触发复查。稀客 `autoPrep*` 与普客 `autoNormal*` 的送酒、开锅、送料理、评价和出错暂停必须独立保存、独立传参、独立推进。所有自动开锅都必须登记 generation/content revision job 作为防重复回执；只开启“开始料理”时 job 进入手动交接模式，绝不送达、入箱或复位厨具。手动交接 job 仍按同一目标抑制重复开锅，但成品离开后不得继续预约 controller，也不得因其他目标复用该 controller 而删除。稀客与普客开锅仍共用同一轮厨具预约表；兼容厨具被 Mod 预约、游戏占用或挑战机制暂时锁定时，只返回带正数退避的局部 `waiting/cooking-cooker-waiting`，不得增加阶段重试次数或阻塞其他厨具类型的订单。
- 跨帧料理所有权统一由 `AutomationCookingJob` 表达，不再使用前端布尔、创建时间或单个 Result 指针猜测。job 只保存 `controllerIndex + controllerIdentity + gridPosition` 预约、native pointer、SetCook generation、content revision 与 ChosenRecipe identity，不得保存跨帧 `CookController` wrapper。`RuntimeCookingGenerationTracker` 必须精确被动观察 `CookController.SetCook(Sellable, Recipe, bool)`、`Extract(Action<Sellable>)` 和厨具内容换入 `Store(Sellable)`：`SetCook` 产生新的 generation，三类原生事件共同推进 content revision；prefix 先登记未完成 mutation，只有同一 revision 的 postfix 在 `__runOriginal=true` 且原方法正常返回时才能标记完成，嵌套或后续 mutation 不得被旧 postfix 覆盖，全部回调必须 no-throw。job 在自身 `SetCook` 返回后立即捕获 `LastMutation=SetCook && MutationCompleted=true` 的 snapshot；以后每次读成品、送达、入箱、复位、availability 通知或 `AfterPlayerExtract` 前，都从当前 `AllCookers + LockedCookers` fresh-read 同一预约并复核 native pointer、坐标、锁定、`CouldOpen` 与内容所有权，wrapper 只在当前同步调用栈使用。预约坐标若已位于 `LockedCookers`，必须在读取该 controller 的任何 Cooker/Phase/Result/ChosenRecipe/CouldOpen 状态前立即拒绝。`AfterPlayerExtract` 可在内部合法调用 `SetCook` 开启下一锅，正常返回后不得再要求旧 generation/content revision/Extract receipt，只能 fresh 复核经营代际和同一订单；异常返回仍按不可逆结果不确定处理。Hook 不得主动调用或重放这些入口，Mod 也不得读取、跟踪或操作 `IzakayaTray`。generation/content revision 均未变化时，游戏原生 `FinishCooking` 替换同锅 `Result` 仍属于本 job，并继续沿用既有完成阶段和精确成品身份逻辑。新 `SetCook` generation 发布 `interrupted/cooking-controller-reused`，外部 `Extract` / `Store` content revision 或厨具稳定回到严格空闲态发布 `interrupted/cooking-ownership-lost`；两者都只释放旧 job，不得读取、送达、存储或复位当前内容。自动送达开启时前端只通过既有有界玩家干预回退重新准备，不消耗阶段重试预算；自动送达关闭时保留目标绑定的手动交接回执，但不预约已空闲 controller，也不因其他目标复用 controller 而删除。后续选锅和扣材料前复核必须共用同一纯分类器，只接受严格空闲，或 `Phase=Idle + Result=null + CouldOpen=true + ChosenRecipe!=null` 且最近 mutation 为正常完成 `Extract` 的残留状态。对于非空 `ChosenRecipe` 的残留例外，`Store` / 新 `SetCook`、未完成 mutation 或所有权不可读全部 fail-closed；标准严格空闲锅不要求已有所有权快照。除现有自动开锅路径调用一次 `SetCook` 外，Mod 不得主动调用或重试 `Extract`、`Store`、`FinishCooking` 等非幂等写入口；仅 Result 暂时为空且没有所有权事件或稳定空闲证据时必须有界等待，不能据此判定所有权已经丢失。
- job 的停滞和送达超时必须使用有效运行时间时钟，只累计游戏处于可推进夜间经营且前后观测都 eligible 的区间；暂停、场景不可读、控制器暂不可达和断线期间不消耗预算。烹饪阶段只在 phase 或 progress 真正前进时重置停滞钟，普通 waiting、等待原生完成和 API 轮询不能伪装成进展；停滞会保留旧锅，必须 blocked 并要求人工确认，不能自动重开。一般订单料理完成后默认直接送达目标订单；血池地狱 BOSS 成品必须先复核原订单、实际 ID、当前特殊目标签名以及执行目标的严格/受控许可。料理送达与订单完成两个阶段同时开启时，匹配成品进入专用精确无界面结算；任一开关关闭时进入手动交接。job 仍拥有厨具成品且尚未进入手动交接或最终提交时，非目标成品、已确认的完整目标签名变化、Tag 不可读、严格方案 Tag 不符或目标连续不可达才允许进入 `StoreFood` 恢复事务；受控推进只允许已确认可读的 Tag 未全命中，不改变其他恢复条件。匹配成品进入手动交接后不得送达、入箱或复位旧锅，进入专用结算的最终 setter 后则不得再回到入箱或普通重试。同一稳定执行目标下的非目标成品消耗该订单回退预算；`reasonCode=cooking-target-changed-stored` 只表示旧锅因已确认的完整特殊经营目标不同而入箱，必须保留最后一个非空预算所有者和计数。只有随后确认“旧非空签名 -> 不同新非空签名”时才退休旧预算、从零记录新目标并写入结构化诊断；短暂空目标不得提前清零或制造重复退休事件，也不得把游戏正常轮换累计成暂停。厨具成品身份必须区分成员读取失败与精确 signed ID：只有 `Sellable.Type=Food` 且 ID 非负，或 ID 精确等于游戏原生黑暗料理 `-1` 时才是可跟踪成品；`-1` 只能停留在 CookController Result/保温箱恢复边界，不能进入料理目录、推荐或 `RefFood`。Phase 2 读到 `-1` 继续等待原生完成，Phase 3 再按非目标成品入箱并受最大回退限制；其他负数、错误类型或成员读取失败继续 fail-closed。订单最终字段 setter 和 `StoreFood` 都是先写数据、后执行回调的非幂等提交点；正常返回或异常后读到同一 Sellable 对象都代表 committed。`StoreFood` 一旦实际进入方法，异常后对象不存在也不能证明无前置副作用，必须 uncertain 且绝不能再次调用。commit 后只允许对同 generation 厨具有界 cleanup，并且必须精确确认 `Phase == Idle`、`Result == null`、`ChosenRecipe == null`；任一字段不可读都不是成功。
- 自动化部分送达后恢复顾客耐心时，调用 `GuestGroupController.AddPatient` 前必须读取同一控制器的 `CurrentPatient` 和 `MaxPatient`，并把恢复值限制到剩余耐心空间内；若已经超过上限，只能用 `SetPatient(MaxPatient)` 校正一次。不得直接传入固定恢复值，因为游戏原生 `AddPatient` 不裁剪上限，`GuestTableDisplayer.UpdatePatient` 会在裁剪显示比例前使用 progress 索引贴图数组。
- 普客自动化的可执行控制器绑定必须保留 `NormalOrderRuntimeCapture` 的 `GuestGroupController.PushToOrder` 和 `GuestsManager.SetManualControllerOrderInternal` 捕获来源；HUD / `OrderController` 是订单是否可见的事实来源，但只有合并到捕获控制器或启动扫描控制器后才能执行送达、耐心恢复或评价。优化掉帧时应限制刷新范围和轮询频率，不得删除该绑定链路。普客捕获版本变化后只强制刷新普客订单快照，不应重跑完整稀客经营扫描；捕获快照已有可解析订单时，也必须用 live 可见订单做存在性校验。普客特殊经营执行目标选择必须走 registry 统一入口并使用有界语义缓存；前端自动化热路径还必须通过 Worker 预计算该结果，厨具预约、资源视图和实际自动化执行只复用同一轮结果，不得各自重复跑一遍完整目标搜索。
- 本地 API 快照生产必须走统一 dirty-domain 发布器。场景、运行时、稀客经营、普客订单、特殊经营和自动化变化只标记脏域，由控制器统一节流、刷新、签名去重和发布；不要在新增路径中直接散落调用快照发布。普客快照的读取门禁是夜间经营场景可读，不是稀客经营上下文已稳定；`OrderController.GetShowInUIOrders()` / HUD `OrderingElement.ActiveOrder` 已能提供 live 普客订单时，应允许 `NormalBusinessContext` 进入快照，避免必须通过切换页签或手动刷新才能驱动经营中页面和自动化。
- 特殊经营场景通过 `RuntimeSpecialBusinessContextService` 发布 `specialBusiness` 上下文。挑战显示名必须读取游戏 `NightSceneDirector.ChallengeType` 枚举成员的 `UnityEngine.InspectorNameAttribute`；规则注册表不得维护名称映射，元数据失败时只保留有效 challenge type、原始 `ChallengeMode` 和明确诊断。不得用 `DataBaseLanguage.GetMissionLanguage(challengeType)` 猜测标题。上下文必须显式发布 `challengeTypeAvailable`；挑战类型读取失败时立即清除旧 HUD 目标，并让推荐、自动化、游戏界面置顶和高亮全部 fail-closed，不得把无法识别的特殊经营当作普通经营继续处理。
- 特殊经营文档和界面必须明确区分“游戏原生规则”“Mod 执行策略”和“尚未确认的信息”。未经 Assembly-CSharp、IDA 路径、interop 与实机日志交叉验证的结论只能记录为待确认，不能进入推荐硬条件或自动化。
- 特殊目标不写入 `RecommendationState`。目标营业额、符卡计数和未确认结算规则只展示；只有已确认会影响评价或挑战进度的条件才能进入经营中推荐。进入推荐的料理目标统一表达为 `SpecialBusinessFoodTargetPolicy`：`enforcement=none|require`、`match=any|all` 和规范 Tag 数组，场景模块不得为旧字段或旧语义保留兼容别名。完整规则和证据记录在 `docs/special-business-scenes-notes.md`。
- 怪诞料理大赛第一阶段按喜好 Tag 命中数选择；第二阶段保留原订单料理/酒水，并要求预估 `ExGood` 与当前目标 Tag；第三阶段分身继续采用同一保守约束，但必须在文档中注明这是比游戏原生最低门槛更严格的 Mod 策略。古明地恋本体护盾期使用揭示的正面料理、厌恶料理和酒水 Tag，破防后保留原订单并按剩余目标分、预算与剩余提交次数规划，不绑定轮换目标 Tag。
- 怪诞料理大赛第三阶段不能只凭 `guestId=2006` 判断古明地恋本体。分身优先读取 `GuestControllerSpawnType=GhostInChallenge`；本体还需结合阶段、订单类型、手动控制器状态和真实 controller 绑定。开锅前后必须校验阶段/目标 Tag 签名和成品 Tag；失败组合由前后端共同抑制，跨帧 job 必须保留 `specialBusinessRole=wacky-koishi-boss`。
- 血池地狱第二阶段混合生成 `NormalOrder` / `SpecialOrder`，第三阶段生成 `NormalOrder`；生成与集合委托可能按声明返回类型把具体对象暴露为 `OrderBase`。分类必须通过共享 IL2CPP cast 入口分别尝试两种具体类型，并且只接受恰好一种转换成功；随后严格读取 `NormalOrder.Guest` / `SpecialOrder.SpecialGuests` 的 `GuestBase.Id`，再与 controller 的 `OrderingGuest.Id` 精确复核 canonical BOSS ID `1003`。双重成功、均失败、成员缺失或身份冲突一律发布 `yuuma-order-unverified` 并 fail-closed；禁止用角色名、订单文本、`ToString()`、宽泛成员、类型包含判断或把所有 `OrderBase` 放行作为回退。
- 血池地狱只在 `challengeType=Story_BloodPondHell` 且订单角色精确为 `yuuma-boss-order` 时启用双 Tag 规则，规范策略仍固定为 `require + all` 和两个完整动态料理 Tag。`SpecialOrder` 保留原点单并且只接受全命中严格方案。显示在普客区域的 BOSS `NormalOrder` 保留原料理与原酒水，先搜索全命中严格方案；严格方案为空时，仅允许从同一原料理和原酒水、且已通过料理/酒水解锁、库存、材料、厨具、排除项及订单强身份等候选硬门禁的结果中选择受控推进方案。受控推进不是 `any`，不会在目标缺失、目标不可用或硬门禁失败时生成计划。只有精确非 BOSS 订单继续使用普通规则。
- 在判断严格方案为空之前，Yuuma 双 Tag 搜索必须按目标的 `matched/reachable/blocked` 状态保留可达分支，不能让通用有限宽度 beam 裁掉严格解。该搜索选项只允许 Yuuma `NormalOrder` 使用；不得为受控推进改变普通、幽幽子或怪诞料理共用的候选比较器。
- 受控推进请求必须显式携带完整预测 Tag 列表，且规范化后的预测结果确实未满足当前双 Tag；预测已全命中却携带许可时拒绝。开锅前严格方案要求预测 Tag 全命中，受控方案仅在该显式许可、原订单项目和当前策略仍全部成立时允许预测 Tag 未全命中。
- 血池地狱 HUD 目标捕获只允许被动 Hook `IncomeControllerYuuma` 已验证的六个更新入口，所有回调使用 no-throw 外壳、当前夜间经营 generation 归属和有界诊断。禁止调用或 Hook 伤害、怒气、目标刷新、阶段循环等会直接推进原生挑战的入口。跨层特殊料理目标必须使用精确 `challenge/owner/generation/foodTags/matchMode/signature` 契约：怪诞料理保持 `Any`，血池地狱只接受 `owner=yuuma`、当前经营 generation、`All` 和恰好两个完整 Tag；不得恢复 Wacky 专用 wire 字段、请求回退或旧目标签名。完整 Yuuma policy 与 revision 必须在同一锁内原子读取；revision 只在两个非空、去重后仍恰好两个并按 Ordinal 排序的 Tag identity 真正变化时递增，重复发布、Tag 顺序变化和 `A -> empty -> A` 均不递增，`A -> B -> A` 的两次真实变化均递增。policy 或 revision 暂不可用时，本轮不能入箱或重开，持有成品的 job 有界等待。`allowYuumaControlledProgression` 是执行计划、本地 API target、后端 job 与快照中的独立许可，不属于策略 signature/revision；严格目标和受控目标必须视为不同 job identity。它只允许精确 Yuuma BOSS `NormalOrder`，且请求的 `foodId == matchFoodId`、`beverageId == matchBeverageId`。该 BOSS 订单的推荐首项、自动化、游戏界面置顶、料理/材料/酒水列表项高亮、目标厨具高亮、目标桌位高亮和目标订单高亮共享 `executionPlans[0]`。自动化只对强身份复核通过的 `yuuma-boss-order` 计算推荐、送达酒水和开锅；成品完成后必须复核原订单料理/酒水、实际料理 ID、当前策略 identity、精确锅次和实际 Tag。严格方案要求预测和实际 Tag 全命中；受控方案只在策略仍有效且 Tag 可读时允许未全命中。原订单项目不符、实际 ID 不符、Tag 不可读、策略变化或其他门禁失败仍走既有 commit-once 保温箱与安全栅栏。`NormalBusinessOrder.RuntimeGuestId` 必须从订单/控制器已读取的原始运行时身份发布，并由前端原样传给普客完成请求；缺失或不等于 `1003` 时不得用 `GuestId`、名称或 role 回退。匹配成品只有在 `AutoDeliverFood && AutoCompleteOrder` 时进入专用无界面结算；任一开关关闭时保留成品并进入手动交接。`ShouldPlayerThrowDeliver` 只表示 `registeredTimedBuff` 中存在玩家投掷送餐能力，不是单次送餐事务状态，专用无界面结算不得以它为门禁；真实原生送餐并发只按当前订单的 `ServedFoodInAir` / `ServedBeverageInAir` 精确字段无副作用等待，仍禁止打开送餐界面或模拟投掷协程。预检必须锁定同一经营 generation、原生 order/controller 指针、`OrderBase.ManualOrder` 精确 bool、fulfilled getter、最终料理 setter、对应评价入口和全部送达记账入口；手动控制订单只能使用同一次 `SetManualControllerOrderInternal` 为同一订单捕获的回调调用 `EvaulateManualOrder(controller, callback)`，标准订单只能调用 `EvaluateOrder(controller, false, null)`，缺回调、形态不符或身份漂移全部 fail-closed，不允许跨路由降级。事务顺序固定为最终料理 setter、第一次重新取得并完整复核同一订单、同锅次复位、`OnCookerAvailabilityUpdate(-1)` 与同一控制器的 `AfterPlayerExtract()`、第二次 `FindYuumaRuntimeOrder` 与完整复核、fresh fulfilled、精确评价、`StatusTracker.AddBussinessFoodConsumes`、`PartnerManager.OnOrderBaseStatusUpdate(FoodDelivered)` 与 `TryAddPlayerOccupiedDeskCode`。酒水专用入口不得接收外层通用 `RuntimeOrderMatch`，必须从稳定 target 内部构造请求并以 `YuumaSettlement` purpose fresh lookup；库存预检唯一解析 `BeverageOut(int,bool)` 和两个 `IEnumerable<int>,bool` range 方法。所有非零库存值，包括原生无限哨兵 `-1`，都执行缓存 MethodInfo 的完整 `BeverageOut + BeverageInRange/BeverageOutRange` 序列，由游戏 API 保持无限哨兵；有限库存显示按免费净零或额外消耗全量计算。实际项目 `get_Tags()` 只接受精确 `Il2CppStructArray<int>`，其他容器形态 fail-closed。酒水送达同样补齐对应 beverage consume/status/desk 通知。评价前必须缓存记账所需对象、方法、枚举上下文和桌位，评价返回后不得重新读取可能已经释放的 IL2CPP wrapper，缓存 order 只允许作为 `OnOrderBaseStatusUpdate` 的不透明参数。每个 cooking job 只保留小型单调 `YuumaSettlementTransactionTracker`；任一不可逆阶段进入后结果不确定即转为终止人工确认栅栏，禁止重放。此前破坏第三阶段的是缺少完整原生顺序和路由复核的通用直评，不得在该专用入口之外调用评价，也不得恢复旧的大范围 finalization gate/coordinator、consumed/ACK 特例、HUD 进度 identity、送餐面板/UI 模拟、生成闭包、协程、托盘或 `MoveNext` 路径。
- 幽幽子第二阶段只在稀客标准评价达到 `ExGood` 并触发正面符卡后推进；候选必须满足当前稀客料理/酒水点单、避开该稀客自身的厌恶，并在排除点单 Tag 后命中至少 `2` 个额外喜好 Tag。不得把幽幽子三阶段规则用于第二阶段。
- `Story_Yuyuko` 三阶段的主幽幽子使用独立的 `story-level-sum` 模式，按已送达料理与酒水等级合计评价：`>= 2` 为 `Normal`、`>= 5` 为 `Good`、`>= 8` 为 `ExGood`。`progress` 保留原订单料理/酒水并要求等级合计至少为 `5`；只能达到 `Normal` 时按精确原订单清理且不承诺推进。
- `Challenge_Yuyuko` 三阶段必须按运行时订单形态选择评价模式，不得复用剧情版等级合计规则。稀客 `SpecialOrder` 使用 `retake-tag-order`：以最终料理与酒水的完整 `Tags` 走标准点单评价，满足两项请求 Tag 后基础分为 `2`，排除点单 Tag 后的当前稀客额外喜好/厌恶继续影响结果；推荐侧按每个额外喜好至少 `+1`、每个厌恶 `-1` 计算保守下界，原生 `W2` 喜好只会让实际评价高于该下界。结果 `>= 3` 才推进，`>= 4` 的 `ExGood` 优先；候选不得被收窄为某个精确料理/酒水原订单。
- 重修三阶段的精确料理/酒水 `NormalOrder` 才使用 `retake-food-modifiers`。执行目标必须保留游戏生成的原料理与酒水，精确匹配先建立 `Normal=2`，后续只统计实际生效的厨具/价格/大份等动态料理 Tag，以及相对原配方新增且实际生效的额外材料 Tag；原料理基础 Tag 不得再次作为 modifier 计分。候选先收窄到原料理/原酒水，并单独生成、合并无加料原菜；结果 `>= 3` 才以 `progress` 推进，等于 `2` 时以 `refresh` 清理但不推进，结果 `< 2` 必须阻断。
- 重修三阶段的 `NormalOrder` 必须在调用可能开锅的本地 API 前，按当前经营 generation 锁存完整执行目标；后续送达、评价和厨具容量估算都只使用同时匹配经营 generation 与规范特殊目标签名的同一锁存目标，不得因响应时序、库存或同场目标轮换改用 Worker 新目标。特殊模块认领的订单只要仍有送酒、开锅、送达或评价副作用意图，缺少当前锁存目标就必须整单 fail-closed，不得借“本轮无需开锅”继续普通动作。局部关闭普客自动化或全部普客处理阶段时，应保留仍绑定锁存目标或 cooking job 的状态；关闭自动化总开关仍通过取消屏障清理。评价前必须同时要求成品 `Modifier` 加料 ID 与请求 extras 精确全等，并以 `Sellable.Tags.Except(RawTags)` 重建原生 addedTags，与锁存的 `expectedFoodModifierTags` 精确全等；任何读取失败或不一致都 fail-closed。该 modifier-only 契约不得套用到 `SpecialOrder`。
- 进入幽幽子原生评价查找前先用 Completion 精确对象检查 `IsFullfilled`，未送齐只返回正常等待。NativeEvaluation 必须 fresh read 当前订单：`SpecialOrder` 优先复核 capture，必要时才让显式幽幽子专用 manager 扫描候选通过同一验证器；`NormalOrder` 只接受当前 capture，并再次复核 `PeekOrders()` 与原生指针身份。两类路径都必须验证当前经营 generation、强身份、controller 所有权、fulfilled、已送达目标和回调绑定。剧情版只复用最终选中的同一原生 order/controller 捕获记录中的 `onEvaluate` 调用 `EvaulateManualOrder`。重修版不得按挑战类型统一选择入口：精确 `SetManualControllerOrderInternal` 建立的回调绑定必须与后续读取的瞬时 `OrderBase.ManualOrder` 分开保存，并在同一活动订单的状态合并中保持到精确移除、过期或经营代际清理；来源与绑定状态不一致、空回调或多个不同回调均视为冲突。同一 order/controller 的稳定 `ManualOrderSet` 绑定必须同时具备精确 `DisplayClass16_10 + b__77/b__78` 手动回调和主幽幽子 `_50` 回调，才调用 `EvaulateManualOrder`；捕获明确无手动绑定、具体订单已唯一解析为 `NormalOrder` 或 `SpecialOrder`，且 controller 仅具备组订单 `_70`、不具备 `_50` 时，才调用 `EvaluateOrder(controller, false, null)`。`无手动绑定 + _50`、`手动绑定 + _70`、回调缺失、候选歧义或身份漂移全部 fail-closed，禁止换另一入口重试。`SpecialOrder` 门禁严格核对实际加料，并确认请求料理/酒水 Tag 存在于实送成品的完整 Tag 数组；完整喜恶集合与最终档位仍由游戏原生评价计算，不伪造前端预测集合。`NormalOrder` 门禁则校验 exact extras 与 `expectedFoodModifierTags`。对应回调、送达目标或评价语义不可确认时必须暂停。
- 挑战/BOSS 订单可能表现为精确 `NormalOrder` 或 `SpecialOrder`，必须通过统一分类器标记归属。跨帧待办、自动料理 job 和运行时事件必须保存原订单 match 目标、实际 execution 目标、挑战类型、订单角色、规范特殊目标签名与受控推进许可；日志记录 match/execution 对照、严格/受控模式、回调证据和阻断原因。血池地狱自动制作前必须从当前强匹配订单重新唯一确认具体形态，再使用对应 live 查找器取得同一订单，不得在 `NormalOrder` 与 `SpecialOrder` 之间模糊回退。酒水本轮确认提交后立即结束响应，由下一轮 fresh read 后再开锅。匹配料理出锅后继续复核 controller 所有权、canonical ID `1003`、原始点单身份、实际成品、当前策略、严格双 Tag 或受控许可、经营代际和双阶段开关；只有专用结算入口拥有血池地狱最终料理送达、评价与原生状态通知，普通完成入口不得接管或补做其中任一阶段。
- 特殊经营模块应拥有自己的规则构造、普客执行目标选择、目标适用 predicate、订单角色识别和失败组合 key 生成；目标适用性必须复用该模块已验证的阶段与角色条件，不能从 selector 是否存在推断。`special-business/registry.ts` 只负责按 `challengeType` 分发，不应重新承载场景分支。前端规则构造放在 `special-business/rules/`，普客执行目标放在 `special-business/normal-targets/<scene>.ts`，`rules.ts` 和 `normal-targets.ts` 只作为稳定导出入口。C# 侧订单分类、运行时匹配策略、Boss 评价入口、`AutomationCookingJob` 出锅校验和诊断 helper 应放在 `mods/bepinex/src/Save/SpecialBusiness/` 下的场景策略文件中；`RuntimeOrderPreparationService*.cs` 只保留通用自动化编排和跨场景共享动作，怪诞料理大赛运行时判定集中在 `WackyCookingCompetitionRuntimePolicy`。
- Mizuchi 模块只匹配精确 `Story_Mizuchi` 与 `Story_Mizuchi_1/_2/_3`。`SpecialOrder` 必须通过唯一评价 delegate、单一 invocation、精确 callback method、Target/parent closure、selected/group/controller/order guest 原生身份、当前 controlled guest，以及 challenge-specific 的控制类型、目标材料和基础挑战标志共同分类：基础场景要求 `targetIngredientId=5002`、`isMizuchiChallenge=true`、活动 control 属于 `0..2`；三场试炼要求 `targetIngredientId=5005`、`isMizuchiChallenge=false`，活动 control 分别精确等于 `1/0/2`。运行时稀客身份使用与正式目录一致的非负 ID 域，`0` 是莉格露的合法 ID；order/controller/group/selected 必须接受 `0`，controlled 只允许唯一空值 `-1` 或非负 ID，其他负数 fail-closed。控制状态只接受精确 `(-1,None(3))` 无目标期或合法 `(nonnegative,active-control)` 活动目标期；不得缓存上一目标、接受任意 `None`、把 `0` 当空值或用正数门禁排除它。捕获入口声明的 controller 是 `GuestGroupController`，实机可因此交付基类 wrapper；读取 `SpecialGuestsController.SpecialGuest` 或其继承 callback 前必须使用正式 IL2CPP cast 得到精确派生 wrapper，并要求原 wrapper、派生 wrapper 与闭包 `group` native pointer 全等。不得直接在基类 wrapper 上读取派生属性，也不得调用回调、Hook `MoveNext`、扫描场景或使用名称/文本/弱成员兜底。基础和试炼必须使用不同 wire role，但共用唯一 Mizuchi 身份读取与自动化策略；基础附身角色强制预置 `5002`，试炼附身角色强制预置 `5005`，对应普通角色禁止自己的目标材料，跨场景 role 或材料均拒绝。特殊经营模块提供的附身优先级必须由页面排序、自动化选择和游戏界面目标共同消费，不得改动无上下文的普通排序。自动化在请求匹配、扣料前、`SetCook` 前、酒水/料理 setter 前和评价前 fresh 复核同一角色与 lifecycle；最终 `Food.Modifier` 必须与请求 extras 精确全等，并恰好包含或禁止本场目标材料。评价流程必须先读取同一订单的 `IsFullfilled`：只送达酒水、料理尚未完成时返回正常等待并继续开锅，不得提前读取空 `ServFood` 或建立安全栅栏；只有 fulfilled 后才通过共享评价器的 `fulfilledPreflight` fresh 复核角色、闭包、`ServFood` ID 与 Modifier，并紧接着调用原生评价。首次不可逆操作前的漂移必须取消本次动作；扣料后的漂移进入既有 `cooking-start-unowned` 不确定栅栏，酒水/成品送达与评价边界的漂移进入 `mizuchi-contract-mismatch` 人工栅栏。旧 passive `Story_Mizuchi` 登记、trial-only 类型/文件和 `mizuchi-trial-contract-mismatch` 不保留兼容别名；任何阶段都不得降级为 ordinary 或标准经营路径。
- 月都闭包的 control/controlled 组合必须按原生双状态解释：`controlled=-1 && control=None(3)` 是开场及每次捕获后的无目标保护期，此时通过其余 exact 身份与闭包门禁的活动订单全部分类为 ordinary；`controlled>=0 && control=当前试炼类型` 是活动附身阶段，再按 guest ID 精确相等区分 possessed/ordinary。`(-1,当前试炼类型)`、`(nonnegative,None)`、错误活动控制类型及其他混合组合都保持 unverified。不得缓存上一位附身目标、接受任意 `None`，或把无目标阶段当成初始化失败；自动化 fresh 复核在该阶段必须维持既有普通订单，不得因此回收已完成料理。
- 怪诞料理大赛中古明地恋本体破防期的投食方案必须共享同一套评分入口；自动化执行目标、稀客/经营推荐 execution plan、推荐料理和推荐酒水首项都从同一投食分、预算、剩余目标分和剩余提交次数规划结果派生。不得在自动化和可视推荐中分别维护排序公式。
- 普客订单中的客人、料理和酒水名称只能使用本地数据仓库名称或明确可读文本。`GameData.CoreLanguage.LanguageBase`、`Il2Cpp*`、`GameData.*` 这类运行时类型名必须过滤掉；普客订单去重也不得依赖不稳定的客人文本。
- 自动化能力是实验性功能，持久配置必须集中在 `设置 -> 实验性功能`；经营中页只显示运行状态、订单步骤、重试、重置和人工确认入口，不拥有第二套配置。总开关之下，稀客与普客分别由 `autoRareOrderEnabled` / `autoNormalOrderEnabled` 独立门禁；两组共同开关顺序固定为启用处理、自动送达酒水、自动开始料理、自动送达料理、自动完成订单、出错时暂停，稀客收藏限定放在共同开关之后。稀客并发、普客并发、最大重试和最大回退都必须走 `CompanionPreferences`，默认分别为 `2`、`3`、`3`、`2`；稀客和普客都按订单独立判断是否可完成，已满足料理和酒水的订单应优先触发评价，但本地 API 调用仍必须串行 await，避免多订单同时修改 Unity 运行时对象。任一次锁存完成意图的直接送达若成为最后一项，必须在同一个后端调用或跨帧料理 job 事务中 fresh 复核并沿订单的精确评价入口收口；不得删除 job 后依赖前端下一轮补发评价。评价返回后不得再读取调用前缓存的订单 wrapper。新稀客门禁默认 `true` 只用于保持已有隐式调度语义；总开关和各动作阶段的默认关闭仍确保新安装不执行动作。
- 一般稀客和普客必须满足唯一配置不变量：任一 Mod 直接送达酒水或料理为 true 时，同组 `AutoCompleteOrder` 必须为 true。设置 UI 开启直送时用一次偏好更新同时开启完成；关闭完成时用一次更新同时关闭两项直送。持久化读到非法组合时必须 fail-closed 地关闭直送，不能为旧状态隐式开启评价；前端请求层和 C# 运行时服务还要在任何 IL2CPP 读取后的写入、库存扣减、开锅或订单副作用前返回 `automation-config-invalid`。只开启自动开始料理时保留明确的玩家交接模式，不得恢复送餐 UI、托盘、投掷协程或旧完成路径。
- 自动化总开关开启后，前端必须先获取 automation control lease 并持续续约；未取得 automation control lease、连接失败或快照错误时不得发出新动作。automation control lease 的 acquire 必须单飞，显式取消要先等待在途 acquire，避免取消完成后旧续约重新取得控制权。`POST /automation/cancel` 必须显式携带 `target=commands|rare|normal|all`，不保留旧路由、缺参路径或 query 别名：所有目标都在 automation control lease 锁内递增 command epoch，并等待当前已开始命令越过 epoch fence；`commands` 只隔离排队与迟到命令并保留全部 `AutomationCookingJob` 和 automation control lease，`rare` / `normal` 只清理对应类型、尚未进入不可逆事务的 job 并保留 automation control lease，`all` 清理全部可取消 job 后才释放 automation control lease。已经提交或无法确认料理送达、保温箱写入或血池地狱结算的 job 必须保留到清理、精确评价或人工安全栅栏的完整边界，不得因阶段或总开关关闭而中断。前端必须持久记住待确认的 endpoint 与 target；ACK 前可重发取消，ACK 后记录 Mod session 与 command epoch，只轮询同一 session 的 `automationCancellationAppliedEpoch`，直至主线程已应用代际不小于 ACK 代际后才恢复调度。`all` 支配单组请求，`rare + normal` 合并为 `all`，job 目标支配 `commands`。取消屏障只读取当前结构化存储格式，不新增旧配置迁移。临时断线或客户端退出不主动清 job，automation control lease TTL 到期后新控制者取得更高 epoch，并从快照中的活动 job 与递增事件序列接管；旧 epoch 请求和迟到响应不得改变新状态。
- 自动化端点和运行时事件统一返回 `waiting`、`progressed`、`completed`、`interrupted`、`retryable-failure`、`blocked`、`fatal`、`cancelled` outcome，并携带 `stage`、`reasonCode`、`jobId`、`retryAfterMs`。C# 真实 `beverage/cooking-start/cooking-delivery/order` 阶段优先于前端请求前推测。前端失败状态必须保存 `retryStage`；阶段改变或对应阶段开关关闭时只能清除普通失败的计数/退避，不能让送酒失败污染开锅，也不能清除副作用不确定栅栏。只有 `progressed/completed` 能清零当前阶段重试并刷新真实进展时间；`waiting/interrupted` 保留重试计数，`retryable-failure` 有界累加。blocked/fatal 中凡涉及开锅、送达提交、入箱、成品读取或厨具 cleanup 不确定的状态，都必须设置 `manualResolutionRequired`、保留 `prepared` 并禁用普通重试；通常只有玩家处理现场后点击 `确认已处理` 才能解除。血池地狱匹配成品完成后的手动交接是正常 outcome，不得登记为不确定栅栏或要求 ACK。前端用 request epoch 和事件 sequence 拒绝取消前或终态事件前发出的迟到响应，禁止再解析中文消息或使用前端经过时长猜测恢复。
- 厨具出锅结果必须读取 `CookController.Result` 或其精确 backing field，并确认对象是料理 `Sellable` 后才能送达或进入 `StoreFood` 恢复事务。不得通过大小写退化读取 `CookController.result` / `resultVisual`，这些字段是视觉 `SpriteRenderer`。非 `Sellable`、连续不可读、进度回退和 generation/content revision 所有权不可确认都必须形成有界的结构化 waiting/blocked/interrupted 结果；不得无限重试，也不得碰触不属于本 job 的结果。
- 自动化状态机只把实际送酒、开锅、单项送达提交、料理进度前进或触发评价视为进展；“选择订单”“匹配订单”、普通轮询和等待游戏原生完成都不是进展。稀客和普客必须通过 `hasServedFood/hasServedBeverage` 与订单事实校准阶段；只有 `get_IsFullfilled()` 为真、严格读到 `HasEvaluated=false` 时才能进入一次评价调用，该值只作调用前门禁。原生评价无论正常返回或抛异常，只有本次调用期间同步发布、且与 generation、具体订单类型、order/controller 原生指针和 lifecycle 全部精确一致的 `Evaluated` terminal receipt 能证明本次调用已提交；缺失 receipt 一律按 commit uncertain 登记同 lifecycle ACK 栅栏，禁止重放，也不得回读调用前订单 wrapper。job 快照、运行时事件和 `aggregate-mod.log` 必须携带 `jobId`、trace/order key、`transactionStage`、controller/result 指针、generation/content revision、phase/progress、outcome/reason、失败与清理计数、`holdsControllerReservation`、`controllerLeaseReleaseReason`、`orderRuntimeKind`、`orderPointer`、`orderControllerPointer`、`orderLifecycleSequence`、`foodDeliveryEvaluationState`、`foodDeliveryEvaluationAttempts` 与 `foodDeliveryEvaluationEffectiveSeconds`；连续相同日志仍合并为 `repeat`。经营中订单行、快照和总日志必须显示同一 `trace=R-*` / `trace=N-*`，便于逐单关联。
- 稀客订单进入自动化时必须只从当前 `executionPlans[0]` 锁定本订单的料理、加料和酒水；不得在锁定阶段扫描后续计划。后续轮询即使开锅、送酒、库存或排序变化让页面重新计算出新的主计划，也不能改用新首项，除非用户重置该订单或订单自然结束。若锁定料理标注为 `偏好备选`，状态中也必须保留该标识。`只处理收藏料理` 只限制料理动作，`只处理收藏酒水` 只限制酒水动作；对应限定应在主计划归一化阶段处理，找不到满足限定的计划时不执行动作，不得绕过限定。
- 稀客页下拉选项不再依赖本地 API 推荐状态里的稀客进度字段；后端也不再发布这类字段，避免热路径读取 `RunTimeAlbum` 和映射表。
- `GET /rare-guests/invitations` 是 GET-only 严格纯读列表，不保留 POST 别名。BepInEx 783 将目标原生静态成员暴露为同名托管静态属性，候选只从已完成的 base+mapped identity 快照 `Entries` 出发：严格读取并校验 `DataBaseDay.allNPCs` 与 `RunTimeAlbum.RecordedSpecialNPCs` 的 closed generic 具体字典属性。`RuntimeId` / `RuntimeStringId` 只负责精确定位日间 NPC；读取后必须严格验证 `NPC.identity.characterId` 等于 identity source chain 解析出的最终 `SourceGuestId`。候选 API、羁绊读取、已邀请判断和原生邀请写入全部使用该 canonical character ID，并按它合并同一稀客的 base/mapped 形态；禁止把 RuntimeId 传入羁绊或邀请路径，也禁止 runtime/source 双查、双写或失败回退。`all` 要求每个 `RuntimeStringId` 在 `allNPCs` 中精确存在且其 `possibleDestinations` 精确引用数组非空，再按 canonical character ID 精确查羁绊。目的地数组只作为存在日间落点的结构门禁，不读取 marker、不反查或展示地点名称。`current` 额外严格读取 `RunTimeDayScene.trackedNPCs`，只接受 `trackedNPCs[currentMap][RuntimeStringId]` 精确存在的当前地图候选，并只从已取得的 `NPC`、`TrackedNPC` 和玩家状态读取 `overridePosition`、角色身份、`ShouldShowSpecialGuestsInDay`、`currentDestination.spawnMarker`、`NPC.defaultDestination.spawnMarker`、`openStatus`、`restDays`、`showTime` 与 `RemainActions`，按 IDA 伪代码重建字段级可见性判定。`NPC.identity` 由外层 wrapper property 取得，但 boxed blittable `SchedulerNode.Character.characterIdentity` 必须按 exact public declared field 读取，且枚举值只接受 `Special=0` / `Normal=1`；non-blittable `NPC.Destination.spawnMarker` 保持 wrapper property。禁止把 property 和 field 堆叠为兼容读取。单个候选的可见性字段读取异常只禁用该候选并进入诊断，全部当前候选均失败时整轮按运行时不可用处理。原生 `TrackedNPC.ShouldShown` 会调用 `DataBaseDay.RefNPC`，因此两者都禁止调用；`NPC.Destination.None` 的 `spawnMarker` 为 null，也不得替代 `NPC.defaultDestination.spawnMarker` 或以硬编码隐藏标记兜底。`DataBaseDay.GetMapLabelFromSpawnMarker` 会对未知 marker 执行抛异常的 `First` 查找，禁止调用该方法，地点 label 匹配不得成为候选门禁。羁绊缺项返回 `kizuna-uninitialized`，不得生成记录。具体字典和数组只能使用 BepInEx 783 元数据验证的精确查键或索引形态，不得全量枚举，也不得调用 `TryRefreshNPCs()`、`GetMapNPCs()`、`RefTrackedNPCAvailability()`、`GetOrGenerateSpecialNPCKizunaLevel()`、`GetAllNPCKeys()`、场景对象扫描、组合稀客数组或 dummy 初始化。readiness 门禁通过前不得解析 `StatusTracker` 或 `DayScene.SceneManager`；通过后只能从各自直接泛型基类的精确静态 `Instance` 属性读取实例，并验证泛型实参、属性类型和返回对象，不得调用 `RuntimeReflectionUtility.GetSingletonInstance`、本地 `GetGenericSingletonInstance` 或 `FindUnityObject`。静态属性、字典形态、键值类型、NPC identity、羁绊字段或精确单例形态不符时整次查询 fail-closed；当前范围缺少 tracked key 表示该身份不属于当前地图，合法缺少羁绊 key 只禁用对应候选。
- 稀客邀请模块总控按当前客户端 `localStorage` 持久化且严格默认关闭。只有总控开启、连接成功、`runtimeLoaded=true`、`runtimeDaySceneReady=true`、正数 `runtimeDaySceneGeneration` 和有效当前地图同时成立时才建立写入上下文；列表读取还必须要求页面处于 `任务 -> 稀客邀请`。上下文身份必须包含连接代际、规范 endpoint、`current|all` 范围、日间 generation 和地图 label；列表身份在其基础上额外受页面可见性约束。首次开启或进入、范围变化、换图、同地图新 generation、重连和邀请完成后各读取一次，无关快照变化不得反复读取。同一身份收到 `ok=false/runtimeAvailable=false` 或传输失败时只按 500/1000/2000/4000ms 重试四次；成功或确定性业务结果停止，耗尽后必须展示后端 `error/status` 并等待手动刷新或身份变化，失败响应不得伪装成正常空列表。关闭模块、离开分栏或失去有效日间上下文时取消列表请求、重试计时器并清空旧结果；前端必须同时使用 `AbortController`、请求 generation 和操作 ID 拒绝迟到列表结果及旧 `finally`，不得用未记录尝试形成热循环。整批读取异常日志必须包含 scope、读取阶段和完整异常；返回上下文拒绝或成功结果时记录可用的状态与候选诊断。手动刷新只强制重读当前有效列表身份。`POST /rare-guests/invite` 与 `POST /rare-guests/invite-all` 必须携带发起快照的正数 `expectedDaySceneGeneration` 和非空 `expectedMapLabel`，缺参即拒绝；已经发出的 POST 使用独立于分栏可见性的上下文身份，切换分栏不得伪造取消、清除 busy 或丢弃确定结果，写入期间总控必须锁定到请求明确返回。写命令进入 Unity 主线程时先复核 readiness、generation 和地图，再重建最新纯读上下文并复核范围、可见性、羁绊、对话和已邀请状态，每次调用 `StatusTracker.RecordInvitedGuest()` 前还要重复场景复核。任一阶段发生 generation 或地图变化都停止写入并提示刷新；不得只靠前端丢弃迟到响应，不保留无上下文参数的旧写入方式，也不得信任前端旧列表、调用 `DaySceneChatSelectionPannel.InviteSpecGuest()`、使用 `HasTemptInvited()` 阻止候选、直接刷客、推进时间或写 `Story.SpecialGuestControlled`。
- 运行时稀客订单业务投影只接受共享解析器唯一确认为具体 `SpecialOrder`，并由成功 `PushToOrder` 或精确手动 setter 建立 order/controller/native-key 三项完整绑定的捕获项；捕获层必须同时满足七个必需 Hook 和经营 generation 覆盖门禁。native key 只接受非零 IL2CPP 原生对象指针，不得使用 managed hash 或其他派生身份兜底。其他生成、HUD、伙伴与状态回调只允许按同一 native key 更新或移除既有记录，不能创建业务缓存。`GuestGroupController.AllOrders` 与 `AllOrdersData` 指向同一个只累积历史订单的 `Stack<OrderBase>`，不得枚举它们判断当前所有权、查找活动订单或驱动推荐；需要动作前复核时只读取 `PeekOrders()` 当前栈顶，并用共享具体类型解析器和原生指针身份严格比较。当前活跃稀客只补充顾客与预算展示，不是订单存活证据；不得用顾客身份、固定宽限、捕获缓存 TTL 或 HUD 瞬时缺失决定捕获存活。标准 `EvaluateOrder` 与手动评价必须在原方法调用前锁存托管捕获身份，且仅当该身份已 fulfilled、原方法成功返回时清理；`RemoveFromOrder` 与直接调用 `onOrderRemove` 的 `CleanOrderInfo` 在成功返回后按调用前身份清理；`RepellInternal` 在原方法成功返回后清理调用前捕获的订单，不按 `out haveSeated` 的值分支。返回后不得再读取可能失效的 IL2CPP wrapper。`EndDlc4SpecialManualOrder` 只移除 arrival event，不得解释为订单完成或清理。最早进入 Closing 的精确生命周期边界时同步清空捕获缓存；场景名变化只作读取门禁，不拥有第二套延迟清理逻辑。经营中稀客订单行的删除按钮调用 `/orders/rare/dismiss` 清理插件端缓存，不应只做前端隐藏。Delivery 拒绝 `IsFullfilled=true`，Completion/NativeEvaluation 允许该状态直到精确退出回调成功。一般动作不允许 manager 反射回退；古明地恋本体和幽幽子三阶段保留各自显式命名且严格门禁的 live-controller 专用路径。
- 稀客订单必须把具体类型、原始身份、目录身份与展示分离。`NormalOrder` / `SpecialOrder` 的列表归属只由共享解析器决定：直接具体类型按精确完整类型名接受，声明为精确 `OrderBase` 的 wrapper 必须分别转换两种具体类型并且恰好一种成功；特殊经营 role、类型文本、Tag 形态、订单文本和 `ToString()` 都不能决定订单类型。`SpecialOrder.RequestFoodTag` / `RequestBeverageTag` 两项原始 signed 数值连同桌位和独立 `runtimeGuestId` 构成捕获、实时扫描和跨帧 target 共用的强身份；归一化 `guestId` 只供推荐与特殊经营规则使用。`0`、`-1` 等合法值必须原样保留，两项 raw ID 任一不可读或冲突时 fail-closed。展示文本的唯一来源是完整运行时 signed Tag map；映射缺失时只写有界诊断并拒绝业务投影，不生成 `#id`、不从文本反向解析，也不调用 `SpecialGuestsController.GetOrderFoodText/GetOrderBevText`、override 委托链、`SpecialGuest.Get*TagText` 或任何 `ToString()`/文本回退。不得用 `foodRequest` / `beverageRequest` 字段猜测身份。同一 native slot 与同一 lifecycle 内，后续读取的 food 或 beverage raw ID 只要与捕获值冲突，就必须删除该 capture、使该 lifecycle 失效并拒绝发布任何替代 identity、trace 或订单；不得合并新值，只有后续成功的原生创建绑定分配新 lifecycle 后才可恢复投影和自动化。
- 稀客和普客自动化诊断都必须按订单 key 展示，不只依赖长文本日志。每笔订单需要显示步骤、已处理阶段、重试/回退次数、最近原因和人工确认状态，并提供单笔重试和重置。普通重试只解除该订单暂停并保留已完成阶段；普通重置只重建该订单本地判断。副作用不确定时禁用重试并把重置入口改为 `确认已处理`；即使订单已从快照消失，也必须在独立待确认列表保留 ACK 入口。确认必须由当前 session 的 automation control lease 所有者提交准确 barrier sequence，并按后端返回的 acknowledged sequences 清理本地 latch；失败时保持阻断。三种操作都不得影响其他订单或自动化总开关。
- 经营中页的调试 `诊断` 视图需要显示自动化资源状态：`预计厨具占用` 按当前已确认且后端分类可用的 `controllerIndex` 实体槽位展示普客和稀客的本轮预计占用；多类型实体在各类型容量概览中可以出现，但真实预约只能占用一次。特殊经营普客必须复用 Worker 预计算的执行目标结果；预计差评、目标不一致或回调链不完整等 blocked 订单只显示为未占用厨具原因，不得计入预计容量。该投影只在诊断视图可见时计算，仅用于排查和用户判断，不应反向驱动自动化状态机。
- 稀客订单完成前必须同时校验料理和酒水送达状态，不要只返回或处理第一个缺失项。如果快照已有绑定该订单和目标料理的活动 `AutomationCookingJob`，前后端都应消费其结构化状态，不得因厨具冻结或 Debuff 重复开锅；job 以 interrupted/completed/cancelled 终止后再按订单事实决定重新开锅或推进下一阶段。
- 自动开始料理固定尝试完成原生 QTE 奖励结算，不再提供跳过或完成 QTE 的配置开关。该功能不打开游戏音游面板，只尝试调用游戏 QTE 成功奖励入口；运行时失败时返回诊断信息，不应中断已开始的料理。
- 游戏内料理/材料/酒水列表置顶及对应列表项高亮是实验性功能，只允许在对应面板刷新作用域内影响游戏原生 `CheckPinned` 判定，并在精确元素绑定回调中增加可见提示；不得直接改写 IL2CPP 列表、自动点击或绕过游戏自身筛选。本地 API 目标必须携带源订单/主方案 `targetRevision`，且该 revision 必须进入发布判等和运行时事务身份，同桌同搭配的新订单也必须重新发布。制作页加料选项必须由用户选择的 exact 变体行登记事务。列表注入、CookCount、行重建、迟到视觉诊断及 submit/output preflight 只读 selection state；完整 imported Recipe 只在最外层 `WorkSceneCookingSelectionPannel.UpdateAllVisual` prefix 内用于初次原生基础配方导入、追加 extras 后的同次复核和换菜 staged receipt。最终目标复核、一次 `IngredientOutRange` 和精确 List 写入必须持有同一 target publication lock，任何不可逆结果不确定都禁止重试或猜测回滚。最终开锅只能由 exact output closure prefix/finalizer 事务化确认，Mod 不直接调用 `SetCook`。
- 制作页刷新必须区分入口。目标变化直接调用的 `UpdateRecipeField` 只重建 recipe surface：`Applied` 在 fresh authoritative Recipe 与 selected-ingredient receipt 一致时跨 target snapshot 保留；`OutputReady` 还须 exact output receipt 一致，也可在目标集合变为空时保留同一 transaction sequence 和 origin identity。完整 `UpdateAllVisual` 会继续重建 output surface，因此其 outer prefix 必须在原生 `UpdateRecipeField` 运行前 tombstone 稳定 `OutputReady` 的旧 closure/final callback、清空输出标量并退回 `Applied`，等待 fresh rebind；只有 exact armed switch 可把撤销交给同一次 switch receipt。`Applying`、`OutputPending`、`OutputSubmitting`、`Switching`，target snapshot 变化发生在活动 switch attempt 内，或 authoritative/selected/output receipt drift 时，一律进入 `Uncertain`；同一 snapshot 内的正常 switch attempt 仍按 exact identity 随 `UpdateAllVisual` 转移。不得把普通 surface 失败升级成 mutation latch。Recipe 行不得清游戏 callback 或写 `interactable`。output 按钮复用为 Recipe 行时，只有 fresh 证明 callback 仍属于旧 exact Mod output closure 才能清理；无法证明时只退休旧 managed output owner。
- outer prefix 的 reset receipt 通过 ThreadStatic 嵌套 scope 单次交给最近的 inner recipe postfix。消费时须复核同一 PanelState/epoch/transaction、`Applied`、全零 output 标量、旧 `AwaitingRebind` button 与 `Tombstone` closure；若 prefix 后出现 fresh output rebind 或任何 ABA，旧 receipt 立即退休并由 caller 重新处理当前 `OutputReady`，不得短路。
- 目标厨具、桌位、HUD 订单卡片和投掷送达面板高亮统一遵守本节前述运行时视觉通则：所有来源、目标、组件与池化对象必须按 exact identity 唯一闭合，证据缺失或形态漂移时 fail-closed；只能创建和修改 Mod-owned 对象，正常创建、脉冲和销毁只在 Unity 主线程执行，经营 Destroyed、off-main 清理或池化 identity 漂移时只 abandon wrapper。禁止按名称、文本、path、固定 index、浮点容差或备用视觉识别和恢复。精确 Hook、几何、ownership 与禁止项见 [`mods/bepinex/README.dev.md`](../mods/bepinex/README.dev.md)，并由 `tests/runtime-seat-highlight`、`tests/runtime-order-highlight` 和 `tests/runtime-throw-delivery-order-highlight` 三个 smoke 固定。
- 普客订单被动快照应保持约 1 秒级缓存；普客自动化动作后只强制刷新普客订单快照。没有活动 `AutomationCookingJob` 时不要在 `Update()` 热路径轮询料理 job。
- 教学经营自动化门禁只使用 Unity 主线程上的精确 `NightSceneDirector.Instance.IsInTutorial`。当前 Active generation 一旦确认教学就锁存到本场结束，不用场景名、对话、日期、订单或特殊经营标志兜底。属性不可读时 fail-closed。快照字段只控制前端调度和状态文案，后端自动化命令和跨帧 job 必须重新消费同一门禁；暂停时作废前端请求代际，但不改写用户自动化偏好、不清空订单诊断状态。
- 伴随窗口 `日志` 页控制总日志 `aggregate-mod.log`、诊断包导出和本机 BepInEx 控制台可见性，不读取 `BepInEx/LogOutput.log`、自动化独立日志或经营诊断独立日志。总日志每次集中写入前必须在服务锁内确认活动路径存在；外部删除或移走后在下一条日志到达时释放旧句柄并重建原路径，同时重置当前日志的重复摘要和诊断签名。不用 watcher、定时轮询或无事件空文件代替此边界。总日志分片上限不保护 BepInEx/Unity 共享 output log；任何后台 worker 都不得用无限异常重试向共享日志持续写入，Mod 也不得接管、截断或删除共享日志。
- Mod 不得改写全局 `BepInEx.cfg`。控制台启动自动显示默认关闭；默认关闭时不得隐藏用户已由 BepInEx 全局配置开启的窗口。仅回环客户端的显式请求可显示或隐藏当前进程窗口：显示按需调用 #783 `ConsoleManager.CreateConsole()`，以调用前后真实 Win32 window handle 从无到有判定本次新建窗口，只对该窗口删除 `SC_CLOSE`，并保证唯一 `ConsoleLogListener`；BepInEx driver active、窗口存在和真实可见性必须分别读取，隐藏及失败回滚只按真实可见性调用 `ShowWindow(SW_HIDE)`，绝不调用 `DetachConsole()`。窗口切换、Mod 启动偏好写入和响应状态读取必须在同一服务锁内提交，前端按连接身份、请求代际和 Abort 隔离迟到 GET，并把读取错误与动作错误分开保存。`SetConsoleUtf8` 只处理当前已创建控制台的编码。
- 本地 API 提交到 Unity 主线程的库存、订单和稀客邀请读写命令必须有排队上限和明确状态。GET 邀请列表即使回到 Unity 主线程也必须保持严格纯读；POST 单独/批量邀请才允许在重校验后写入。尚未开始的命令超时后必须原子取消，主线程不得晚到执行；已经开始的命令必须返回确定结果，避免客户端在副作用已发生时重试。控制器销毁时先取消并唤醒排队命令，再停止并等待有界的 HTTP handler；每类队列每帧最多执行一个有效命令。
- 运行时库存修改只允许调用当前游戏的 `RunTimeStorage.IngredientInRange/IngredientOutRange/BeverageInRange/BeverageOutRange` 原生入口，并在调用后读取最终数量做精确校验。不得在原生调用异常时直接写 `Ingredients`/`Beverages` 私有字典绕过 callback。
- BepInEx 783 / Il2CppInterop 1.5.3 使用 Dobby 1.0.5 为 IL2CPP 原生方法建立 trampoline；禁止对 `RunTimeStorage.*Out`、`*OutRange` 或 `ObjectOut` 安装 Harmony/Dobby Hook。当前游戏的 `BeverageOut(0, false)` 会命中函数开头的 short Jcc 提前返回，Dobby 1.0.5 重定位该分支时可能跳入函数尾部 `INT3` 填充并触发 `0x80000003`。库存诊断必须放在已受控的调用方或既有业务日志中，不得恢复全局出库入口 detour。
- 诊断开关只能增加采样和日志，不得改变推荐、订单或自动化的权威业务输入。夜间经营稀客订单始终以当前 generation 已就绪的 runtime capture 为唯一正式来源；就绪空捕获是权威空集合，未就绪时整轮 fail-closed，反射探针结果只进入诊断快照。
- 自动更新同一时刻只允许一个检查、下载或安装操作。Mod `UpdateService` 是唯一外网检查者：成功后按 1 至 168 小时配置续检，失败按 15m/30m/1h/2h/4h/6h 封顶退避；Local API 关闭时必须先停止接收新请求，再取消自动、手动检查和下载共享的服务生命周期令牌，等待 handler、活动操作和单实例调度器退出。取消检查必须恢复稳定状态并让下次检查立即到期，不能落盘卡在 `checking` 或沿用取消尝试触发失败退避。服务启动时还必须恢复强制退出留下的 `checking/downloading`，并只清理更新下载一级目录内严格匹配本服务语义版本和 GUID 格式的临时目录。前端只轮询 `/updates/status`，不得直接访问 GitHub 或自动调用 `/updates/check`；活动状态 2 秒收敛，状态读取连续失败按 2s/5s/15s/60s 退避，稳定状态 60 秒读取。状态请求和用户动作使用独立代际；断连或 endpoint/token/连接代际变化必须清理忙碌状态并丢弃迟到响应。全局提示只能非模态展示，延后状态按 endpoint + tag 保存 24 小时，不得自动下载、安装或关闭游戏。`update-manifest.json` 必须严格校验当前 schema、版本/tag/channel、资产名、SHA256 和大小；更新包流式写盘后同时校验长度和 hash。安装只能使用已校验 staged 包中的 updater，且 staged 版本必须高于当前版本，禁止旧暂存包降级安装。Tauri 外链统一使用官方 opener，capability 只允许本项目 Release URL，不得恢复通用 shell command。
- 面向普通用户的伴随窗口默认隐藏调试信息。新增扫描状态、运行时来源、性能耗时、内部订单来源、订单 key、总日志等偏排查内容时，必须受 `CompanionPreferences.showDebugDetails` 总开关控制；该开关默认关闭，并只在 `设置 -> 窗口 -> 显示调试信息` 中开启。
- 伴随窗口信息密度优先通过内部页签控制，不要把所有区块直接堆到同一页面。`概览` 固定使用 `状态 / 库存 / 操作` 分栏；`设置` 固定使用 `窗口 / 连接 / 推荐 / 实验性功能 / 更新` 分栏。连接配置必须集中在 `连接` 分栏；推荐策略、过滤、排序和稀客专注模式默认精简集中在 `推荐` 分栏；自动化配置与两类目标各自的游戏界面置顶、加料料理选项、厨具/桌位/订单高亮集中在 `实验性功能` 分栏。普客、稀客、经营中和稀客订单专注模式的推荐/订单列表应保留稳定内容区域和内部滚动，避免数据从空变有时造成大幅布局跳动。
- `设置` 页的静态解释文案必须通过统一字段帮助原语展示，页面流中只常显设置名称、当前值、操作控件和简短禁用状态。连接状态、实际 endpoint、更新进度/错误、重要操作安全流程、操作结果、运行时不可用信息和平台能力限制属于当前事实或必要操作约束，必须继续常显，不得隐入说明浮层。说明浮层是只读、非 modal 的 `tooltip` 语义：鼠标只在悬停信息图标时打开，键盘或手柄聚焦原设置控件时直接打开，触屏点击信息图标可固定/取消；切换字段、分栏、点击外部或按 `Escape` 时关闭，同时只允许一个浮层开启。普通信息图标不得成为额外手柄停靠点，`A` 仍操作原控件；仅当原控件禁用时，图标才可作为读取说明和禁用原因的唯一焦点。信息按钮必须位于实际 `<label>` 外部，避免嵌套交互和误切换。浮层通过 Portal 渲染并继承主题与背景透明度，必须启用边缘 flip/shift、响应式最大宽度和长文换行，不得建立内部滚动容器、改变页面高度，或在 640px 桌面与 390px 移动宽度下溢出。迁移完成后直接删除旧常驻说明路径，不保留重复展示。
- 游戏内不再保留 IMGUI 面板；游戏侧只负责后台读取、自动化执行、本地 API 和伴随窗口唤起，所有用户交互放在独立伴随窗口。

- 血池地狱跨层策略的 `revision` 是独立于规范 signature 的动作代际。运行时快照、前端 wire policy、Worker/回滚目标、本地 API 请求、后端 target 和 cooking job 必须原样透传；血池地狱只接受与当前同锁读取值精确相等的正 revision，怪诞料理和空策略固定为 `0`。受控推进许可不修改 policy/signature/revision，但必须作为 execution target 与 cooking job identity 的一部分原样透传，禁止严格 job 与受控 job 相互复用。`A -> B -> A` 后第一轮 A 的迟到请求必须在任何副作用前被拒绝。酒水事务固定按 `BeverageOut -> fresh order -> final setter -> fresh order -> 未 fulfilled 时恢复耐心 -> fresh order -> range 调整 -> fresh order -> consume/status/desk` 执行；每个原生回调返回后都要先复核 generation/policy/revision 并重新取得同一订单，只有最后一次 fresh order 才能建立经营记账上下文。
- 血池地狱每次专用订单查询都重新读取当前 `OrderBase.ManualOrder`，并仅在当前值为 true 时按同一 order/controller 原生身份取得 `ManualOrderSet` 回调；捕获时的旧布尔值不决定当前评价路由。酒水入口和每次不可逆调用后的 fresh order 都必须同时读取 `ServedBeverageInAir`，非空时禁止重复扣库、覆盖 setter 或开始最终料理结算；酒水作为部分送达项时按已验证的非手动订单语义恢复一次耐心，回调后再次复核 revision 和订单身份。稀客或普客的自动送达料理、自动完成订单开关从开启变为关闭时，前端必须推进 command epoch 并调用现有 cooking-job 取消栅栏，不能让开锅时锁存的旧意图继续执行最终结算。
- 普客与稀客的两个成功创建 Hook 必须在 `HasEvaluated=false`、`PeekOrders()` 精确栈顶和当前 Active generation 同时成立后，为 concrete kind + order/controller native pointer 分配进程单调的 `OrderLifecycleSequence`；其余五个评价、移除、清理和驱逐 Hook 只发布调用前锁存生命周期的 wrapper-free terminal receipt。Clear 只清状态，不能回退 sequence watermark；同 native tuple 重绑必须推进 sequence，旧 postfix、旧 HTTP 请求、旧 runtime event 或旧前端状态都不得命中新生命周期。三类订单自动化请求必须携带当前快照的正序列，任何游戏副作用前要求 request、fresh capture、active store 三者完全一致；trace、orderKey、桌位、名称和文本都不能代替该门禁。
- `AutomationCookingJob` 的逻辑存活、cooker controller lease 和 evaluation receipt 是三个独立语义。只有 `HoldsControllerReservation=true` 的 job 持有 cooker controller lease 并构成 Mod 厨具预约；人工交接、严格 cleanup 完成或 cleanup 明确终止后 cooker controller lease 只能单调释放，即使 job 仍等待订单评价，也不得继续占用 controller 或阻止其他订单复用。料理送达事务在 cleanup 后清除成品 wrapper，只保留标量 binding token；自身评价正常返回或异常后都必须消费同生命周期的 `Evaluated` terminal receipt，缺失时视为 commit uncertain，进入不可重放 ACK 栅栏。外部精确评价/移除可由同 token receipt 收口；暂时不可读只在 12 次、至少 5 秒且最多 20 秒的有效运行窗口内重试，运行门禁暂停时钟，耗尽后登记同生命周期栅栏并退休。快照与事件必须公开 `transactionStage`、`holdsControllerReservation` / `controllerLeaseReleaseReason`、`orderRuntimeKind` / `orderPointer` / `orderControllerPointer` / `orderLifecycleSequence` 和 `foodDeliveryEvaluationState` / `foodDeliveryEvaluationAttempts` / `foodDeliveryEvaluationEffectiveSeconds`；“保留 job 到评价/栅栏”不得解释为继续占锅。
- 人工安全栅栏只允许按 exact order lifecycle 登记、查询和 ACK。离开 Active 时必须按结束的 business generation 退休全部 barrier 及其保留事件；原生调用栈内触发 Closing/Destroyed 后晚到返回时，不得在清理后回填旧 generation 的 ACK 栅栏，只保留有界 `business-lifecycle-ended` 诊断。当前 Active generation 的未确认栅栏不得用容量静默淘汰。

## 文档维护

- 用户安装和使用写入 `mods/bepinex/README.md`。
- 开发和构建写入 `mods/bepinex/README.dev.md`。
- 机制或运行时读取路径变化时，同步更新 `docs/` 和 `mods/bepinex/docs/`。
- 用户可见功能、快捷键、设置项、自动化行为、页面布局或本地 API 变化后，必须在提交前同步文档。
- 版本发布前如果文档落后，先补文档再提交版本号并合并 `main`。
